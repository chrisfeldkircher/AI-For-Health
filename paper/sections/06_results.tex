\section{Results}
\label{sec:results}

\subsection{Cumulative stack}

Table~\ref{tab:stack} consolidates the controlled-positive pipeline. The cumulative lift from uniform-A2-grouped to A5b K=2 5-seed mean-logit ensemble is $+0.073$ UAR; the K=2 single-system canonical (5-seed mean) is $+0.068$ ($\sim$17$\sigma$ at $N{=}5$ over the leak-corrected baseline).

\begin{table*}[t]
\centering\small
\caption{Cumulative stack on URTIC devel\_test (speaker-grouped sub-split). All numbers $N{=}5$ unless noted. The 2017 baseline 0.7100 is on the hidden test set; we report the apples-to-oranges caveat in Section~\ref{sec:disc}.}
\label{tab:stack}
\begin{tabular}{lcccc}
\toprule
stage & devel\_test UAR & cumulative $\Delta$ over baseline & $\sigma$ & speaker probe LR \\
\midrule
uniform-A2-grouped (leak-corrected baseline)        & $0.6361$            & ---       & $\sim 0.002$ & $0.0760$ \\
A2.5 honesty-prior init                              & $0.6563$            & $+0.020$  & $0.0027$    & $0.0725$ \\
A5b K=1 (A2.5 + G4\_gi)                              & $0.6934$            & $+0.057$  & $0.0064$    & $0.0675$ \\
A5b K=2 single (A2.5 + G4\_gi + G5\_modulation)      & $\mathbf{0.7037}$   & $+0.068$  & $\mathbf{0.0060}$ & $0.0729$ \\
A5b K=2 5-seed MEAN-LOGIT ensemble                   & $\mathbf{0.7090}$   & $+0.073$  & --- (n=1)   & --- \\
\midrule
2017 ComParE Cold baseline (hidden test)             & $0.7100$            & $+0.074$  & --- (n=1)   & --- \\
\bottomrule
\end{tabular}
\end{table*}

The K=2 single LR probe top-1 (0.0729) clears the 0.0780 acceptance ceiling by $\Delta\,0.005$. The K=2 literal 3-d probe is $0.0182 \pm 0.0006$, well under ceiling. Every controlled-positive stage in the stack has both gates passing.

\subsection{Distance to baseline}

The K=2 5-seed mean-logit ensemble at $0.7090$ is $\Delta\,-0.001$ from the 2017 baseline of $0.7100$ — within single decimal-place precision. We report this carefully across the conservative/standard/generous framings in the discussion (Section~\ref{sec:disc}).

\subsection{De-confounding rung verdict summary}

Table~\ref{tab:closure_full} consolidates the negative-result rungs. Every architectural scope tractable in the project's compute budget closes negatively; pure cold-CE is the Pareto-best head-only configuration.

\begin{table*}[t]
\centering\small
\caption{De-confounding rung verdicts on URTIC + frozen WavLM. All gates evaluated at devel\_test under speaker-grouped sub-split.}
\label{tab:closure_full}
\begin{tabular}{lllc}
\toprule
rung               & substrate           & verdict                                 & cold UAR \\
\midrule
A5.5 mixup conservative  & embedding ($\alpha\in[0.70, 0.85]$)  & PARTIAL (UAR floor PASS, probe drop FAIL) & $0.6624$ \\
A5.5 mixup aggressive    & embedding ($\alpha\in[0.50, 0.70]$)  & branch (d): UAR drops, probe doesn't drop & $0.6397$ \\
A6 PoC                   & 128-d L2-norm projection             & strict-gate FAIL; M10 controls show illusory mechanism & N/A* \\
A6b combined-loss        & 128-d L2-norm projection             & monotonic $\lambda$-sweep null; cold-CE Pareto-best & $0.6128$ ($\lambda=0$) \\
A7 PoC                   & 128-d projection + LW DANN           & B\_null per disc-health; mem-gap $+0.91$ at ceiling diag & N/A* \\
A7c v2                   & 4096-d un-bottlenecked DANN          & B\_dann\_dead\_substrate\_resistant; all metrics flat across $\lambda$ & N/A* \\
\bottomrule
\end{tabular}
\\[2pt]
\footnotesize{*A6/A7 measure on the projection $z$, not via a cold classifier; cold-LR-on-z UAR is reported in Section~\ref{ssec:a6}.}
\end{table*}

\subsection{Standalone-UAR predictor — generalises across substrate families}
\label{ssec:standalone_predictor}

The exhaustive K=2 sweep across all six A5a-admitted feature groups + the full 88-d eGeMAPSv02 (G\_egemaps\_full) + a structurally-different foundation-model substrate (HuBERT-base mean-pooled across layers; see Section~\ref{ssec:k3_hubert}) produces a clean predictor: candidates with standalone cold-LR UAR $\geq 0.61$ admit with interior $\beta^*$; candidates with standalone $\lesssim 0.61$ fusion-absorb at boundary $\beta^* \in \{12, 16\}$ with $\tau$ at extreme. See Table~\ref{tab:standalone_predictor}. We propose this as a transferable pre-filter heuristic for honesty-audited late-fusion stacks on small-data paralinguistic corpora — saves $\beta$-sweep compute on candidates that won't admit.

\textbf{Cross-family generalisation.} The heuristic was originally inferred from six handcrafted feature-group candidates (G1--G6 + G\_egemaps\_full). Adding HuBERT-base mean-pooled (3072-d FM-derived substrate) to the test set extends the heuristic across two structurally-different substrate families with the same $\sim 0.61$ threshold. The heuristic is therefore not specific to handcrafted feature engineering — it captures a more general property: ``does the candidate substrate carry a cold-axis distinguishable enough from the calibration noise to fuse productively against the A2.5 anchor's strong logits, vs.\ getting fusion-absorbed at boundary $\beta^*$ with $\tau$ at extreme.'' Any honesty-audited late-fusion stack on a similar-scale paralinguistic corpus can use the heuristic to fail-fast on candidates that won't admit, regardless of substrate family.

\begin{table}[t]
\centering\small
\caption{Standalone cold-LR UAR predicts K-fusion admission across 7 candidates spanning two substrate families. Threshold $\sim 0.61$ partitions admit/fail.}
\label{tab:standalone_predictor}
\begin{tabular}{llccc}
\toprule
candidate         & substrate    & standalone & K-fusion verdict & $\beta^*$ \\
\midrule
G5\_modulation    & handcrafted (64-d)  & $0.6121$ & K=2 WIN ($0.7037$, 5s)         & interior \\
G1\_voicing       & handcrafted (9-d)   & $0.6058$ & K=2 borderline ($0.6964$, 3s)  & interior \\
G6\_spectral      & handcrafted (21-d)  & $0.6053$ & K=2 FAIL ($0.6698$, 3s)        & boundary \\
\textbf{HuBERT-base mean-pool} & \textbf{FM-derived (3072-d)} & $\mathbf{0.5396}$ & \textbf{K=3 SKIPPED (M14 pre-flight, 5s)} & --- \\
G\_egemaps\_full  & handcrafted (88-d)  & $0.5384$ & K=3 FAIL ($0.6801$, 5s)        & boundary \\
G2\_prosody       & handcrafted (10-d)  & $0.5088$ & K=2 FAIL ($0.6674$, 3s)        & boundary \\
G3\_voice quality & handcrafted (14-d)  & $0.5039$ & K=2 FAIL ($0.6576$, 3s)        & boundary \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Recall pattern flip in the ensemble}

The 5-seed mean-logit ensemble produces a markedly more cold-balanced classifier: recC $= 0.791$, recNC $= 0.627$ (vs per-seed single recC $\approx 0.43$, recNC $\approx 0.87$). For a $\sim$9.5\,\%-cold corpus, recovering 79\,\% of cold cases is practically valuable beyond what UAR captures. The recall pattern shift is reproducible across the ensemble re-runs and is a concrete demonstration that K=2 does more than push UAR — it shifts the operating point toward cold detection in a way the per-seed argmax doesn't.
