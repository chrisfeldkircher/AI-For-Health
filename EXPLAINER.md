# EXPLAINER — Cold detection from voice, end to end

A walking tour of what we built, why each step is the right one, and what each feature physically means. Written for someone comfortable with ML basics but new to audio / speech.

> **Audience.** You know what a logistic regression is, what cross-entropy loss is, what overfitting is. You're rusty on signal-processing terminology like *MFCC*, *F0*, *jitter*, *spectral tilt*, etc. — every piece of audio jargon is defined before it's used.

The companion files for raw numbers and decisions:
- [plan.md](plan.md) — the canonical attack plan (what we're trying, in what order, with status flags)
- [summary.md](summary.md) — the live status doc with locked numbers
- [results/](results/) — per-rung JSON dumps and the A5a honesty CSV
- [model/run.ipynb](model/run.ipynb) — the actual runnable cells

---

## 1. The task

**ComParE 2017 Cold sub-challenge.** Binary classification: *does this 8-second speech clip come from a person with a cold or not?* The dataset is **URTIC** ("Upper Respiratory Tract Infection Corpus") — German speakers reading the children's story *Die Buttergeschichte* into a headset microphone in a controlled recording booth. We have ~28k 8-second chunks split into `train` / `devel` / `test`. The official `test` labels are withheld by the challenge organisers, so we use `devel` as our honest proxy.

**Metric: UAR (Unweighted Average Recall).** Average of recall on each class:

```text
UAR = 0.5 * (recall_cold + recall_non_cold)
```

UAR is used instead of accuracy because the dataset is imbalanced (~9.5% cold, ~90.5% non-cold). A model that always predicts "non-cold" gets 90% accuracy but UAR = 0.5 (chance). Cold and non-cold contribute equally regardless of how many examples each has.

**Numbers to anchor on:**
- Always predicting non-cold → UAR = **0.500** (chance baseline by definition)
- 2017 ComParE late-fusion baseline → **0.710** (the number to beat)
- Huckvale 2018 reanalysis on honest test → **0.621** (the *honest* number — see § 4 below)
- Our locked A2 baseline → **0.6428 ± 0.003** (already above Huckvale's honest test)

---

## 2. Audio, the 30-second crash course

### Waveform
A *digital audio signal* is a 1-D array of amplitude samples at a fixed sample rate. URTIC is **16 kHz**, so 8 seconds = 128,000 samples. Each sample is roughly the air pressure at the microphone diaphragm at that instant, normalised to `[-1, 1]`.

### Frames
Audio models almost never look at raw samples directly. We chop the waveform into short overlapping windows ("frames"), e.g. 25 ms long, every 20 ms ("stride" / "hop"). Each frame becomes one column in a 2-D *time-frequency* representation. For our work the convention is **50 frames per second** (hop = 320 samples at 16 kHz = 20 ms).

### Spectrum and spectrogram
For each frame we run an **FFT** (fast Fourier transform) which decomposes the waveform into its constituent sinusoids — gives an array of energy at each frequency. A *spectrogram* is the matrix of those spectra stacked over time. The y-axis is frequency, x-axis is time, colour is energy.

### Mel spectrogram
Human hearing is logarithmic in frequency — we discriminate finely at low frequencies, coarsely at high ones. The **mel scale** is a frequency warping that mimics this. A *mel spectrogram* applies a triangular filterbank on the spectrogram, summing energy into ~40 perceptually-spaced bands. It's the canonical input to almost every speech model since the 1990s.

### MFCC
**Mel-frequency cepstral coefficients.** Take a log-mel spectrogram, run a discrete cosine transform on each frame's mel vector — this decorrelates the bands and packs most of the spectral *shape* information into the first few coefficients. Low-order MFCCs (1–6) describe the broad spectral envelope: how energy is distributed across frequencies. Higher-order MFCCs describe finer structure (often related to pitch harmonics).

### F0 (fundamental frequency, "pitch")
When the vocal folds vibrate (during *voiced* speech like vowels), they do so at some rate — typically 80–250 Hz for adults, depending on speaker, intonation, mood. That rate is the **fundamental frequency, F0**, perceived as pitch. During *unvoiced* speech (like /s/, /f/, /sh/) the vocal folds don't vibrate and F0 is undefined. Algorithms like **pYIN** (probabilistic YIN) estimate F0 per frame and also tell you whether each frame is voiced.

### Voicing manner
A high-level categorical label per frame:
- **silence** — energy below a threshold (the speaker isn't producing sound)
- **voiced** — vocal folds vibrating (vowels, nasals like /m/, voiced consonants like /b/, /d/, /z/)
- **unvoiced** — turbulent airflow with no vocal-fold vibration (/s/, /f/, /sh/, /t/, /k/)

Cold affects voiced and unvoiced frames differently (vocal folds inflame; mucus changes airflow turbulence), so conditioning features on this label is one of our main moves.

### Jitter and shimmer
Two voice-quality measures of how *regular* vocal-fold vibration is:
- **Jitter** — frame-to-frame variability in F0 (the period of the vibration). High jitter means the pitch wobbles. Healthy voice: <0.5%. Pathological voice: >2%.
- **Shimmer** — frame-to-frame variability in *amplitude*. High shimmer means each glottal cycle has a different loudness. Often correlates with breathy voice.

Both rise during illness — colds, laryngitis, fatigue all introduce micro-perturbations into vocal-fold mechanics.

### HNR
**Harmonics-to-noise ratio.** During voicing, the spectrum has a clean harmonic stack (peaks at F0, 2·F0, 3·F0, ...) plus background broadband noise. HNR is the ratio of energy in the harmonics to energy in the noise floor, in dB. Healthy adult voice: 15–25 dB. Cold/hoarse voice: lower (often <10 dB) — more noise relative to harmonic structure.

### Spectral tilt / alpha ratio / Hammarberg index
The slope of the spectrum from low to high frequencies. Healthy modal voice has more energy at low frequencies (rolling off ~12 dB/octave). *Pressed* or *tense* voice has a flatter spectrum (more high-frequency energy). Cold often pushes voice towards a more pressed quality. Operationalised as:
- **alpha ratio** — energy ratio between two bands (e.g. 50–1000 Hz vs 1–5 kHz)
- **Hammarberg index** — peak-to-peak energy ratio between low and high bands
- **slope V0-500, V500-1500** — linear fit of dB-vs-frequency in a band, voiced frames only

### Spectral flux
How much the spectrum *changes* between consecutive frames. High flux = articulation is active (transitions, plosives). Low flux = sustained vowel or silence. Often elevated in faster speech.

### RMS (root-mean-square energy)
Per-frame loudness:
```text
RMS_t = sqrt( (1/N) * sum( x_t[i]^2 for i in window ) )
```
Reported either linearly or in dB (= 20·log10(RMS) + offset). Talking louder → higher RMS. Background silence → low RMS. Doesn't tell you what *kind* of sound it is, just *how much* of it.

### Modulation spectrum
A spectrogram tells you *what frequencies* are present at each instant. A **modulation spectrum** tells you *how fast each frequency is amplitude-modulating over time*. Concretely: take one row of a log-mel spectrogram (the energy in a single mel band as a function of time), treat it as its own 1-D signal, and run an FFT over time. The result is a per-band spectrum whose x-axis is *modulation rate in Hz* — how many times per second that band's amplitude wobbles up and down.

Why it's interesting: speech has natural rhythms at characteristic modulation rates. **<2 Hz** is the breath-group / phrase scale; **3-8 Hz** is the syllable rate (the canonical "speech rhythm"); **10-20 Hz** is the fine glottal / articulatory perturbation scale (close to the regime where jitter and shimmer live). Cold disturbs all three: congestion slows syllable rate, breathiness changes phrase pacing, vocal-fold inflammation perturbs the high-rate band. None of the per-frame static features (MFCC, jitter, HNR) capture these *cross-frame* dynamics — they describe single frames or differences between adjacent frames, not the full envelope FFT. That's the gap G5 fills.

---

## 3. Foundation models for audio (WavLM)

The 2017 ComParE baseline used 6,373 hand-engineered features (the **ComParE feature set**) plus an SVM. That was state-of-the-art in 2017. In 2026 we have **self-supervised foundation models** trained on tens of thousands of hours of speech, which learn rich representations end-to-end without any labels.

**WavLM-Large** is one of these. It's a 316M-parameter Transformer trained on 94k hours of unlabelled speech with a denoising-and-prediction objective. Once trained, it takes a raw waveform and produces a stack of 25 frame-level hidden states (one per Transformer layer) at 50 Hz. Each hidden state is a 1024-d vector per frame. Different layers encode different things — early layers capture phonetic structure, late layers capture semantic / paralinguistic content.

We use WavLM **frozen** — no fine-tuning. The classifier on top is small. This is partly compute discipline (URTIC has only ~9k training chunks, fine-tuning a 316M model would overfit immediately) and partly a methodological choice (so the comparison "what do hand-crafted features add on top of WavLM?" has a clean control).

---

## 4. The shortcut-learning problem (the Huckvale trap)

This is the methodologically important part. The 2017 baseline reported UAR 71.0 on devel and the headline number became 71.0. **Huckvale 2018 reproduced the same pipeline and reported 62.1 on the held-out test set** — a 9-point drop. What happened?

**Speakers.** URTIC has ~210 speakers per partition. Each speaker contributes many chunks. The corpus organisers split train/devel/test so the **speakers are disjoint** between partitions, but in practice the *recording conditions* drift between partitions and so does each speaker's voice timbre, mic gain, and reading style. A model that learns "this voice = cold" on `train` (where some speakers are coldy and some aren't) cannot generalise to a speaker it has never seen, because what it actually learned is "Speaker_42's voice has these formants" — not the disease.

The 2017 number was inflated by **shortcut learning on speaker identity**. Iterating on the devel set picked hyperparameters that exploited devel-speaker idiosyncrasies. The honest number — what you get when you only ever look at the test set once — is much lower.

**Our methodological discipline:**
- Threshold τ is selected on `train_threshold` (10% of train, never seen during training and never the same as devel).
- Model selection is on `devel_val` (50% of devel) early-stopping signal; *never* on `devel_test`.
- `devel_test` (the other 50% of devel) is the one-shot honest UAR estimate. We look at it once per locked rung.
- We track the **val→test gap** every rung. If val and test disagree systematically, we're tuning on val.
- We measure **speaker leakage** explicitly via a probe (see § 6).

This protocol is what gives our A2 number (0.6428) credibility — it survives the Huckvale check.

---

## 5. The split layout (read this once, refer back forever)

```text
train (9505)  ──┬─ train_fit       (~90%, 8532–8554)   ←  fit heads + per-group probes
                └─ train_threshold (~10%,  951–973)    ←  pick τ; pick (β, K) for A5b

devel (9596)  ──┬─ devel_val       (~50%, 4798–4801)   ←  early stopping; honesty audit eval
                └─ devel_test      (~50%, 4795–4798)   ←  one-shot honest UAR per rung

test  (9551)                                            ←  withheld; not used
```

Two split functions live in [model/data/cached_dataset.py](model/data/cached_dataset.py):

- **`stratified_split`** (historical) — per-class random shuffle. Cold rate stays ~9.5% in every slice but pseudo-speakers can appear in both halves of each partition (~198/210 train, 198/206 devel under `split_seed=42`). Used to lock A2/A3/A5a/A5b/A5d.
- **`stratified_grouped_split`** (current methodology, plan §4.5) — `sklearn.StratifiedGroupKFold` keyed on the k=210 pseudo-speakers. 0/0 within-partition pseudo-speaker overlap. Used for A2_grouped (the leak-corrected baseline in `results/A2_grouped.json`).

`split_seed=42` is locked across every rung — same files in every partition, every time. Counts shift by a few files between random and grouped because grouped splits respect speaker boundaries (a speaker's chunks all go to one side).

---

## 6. Pseudo-speakers and the speaker probe

**Problem.** URTIC's 4students release strips speaker IDs. We can't directly check whether the model is shortcutting on identity.

**Solution.** Build pseudo-speaker IDs by clustering speaker embeddings.

1. **ECAPA-TDNN**, an off-the-shelf model trained on VoxCeleb for speaker verification, produces a 192-d embedding per utterance. Two utterances from the same speaker land close in this space; different speakers land far apart. ECAPA was *designed* to be invariant to content and channel — it captures voice identity, period.
2. **KMeans with k=210** (≈ the known per-partition speaker count) on those embeddings → 210 cluster IDs. We treat each cluster as a "pseudo-speaker."
3. **Cross-validation.** HDBSCAN finds 204 clusters independently; KMeans-vs-HDBSCAN agreement is ARI 0.856 / NMI 0.962. Two independent clustering algorithms recover essentially the same groups → the clusters are real speaker structure, not silhouette artefacts.
4. **Negative control.** WavLM-base-plus-sv (a different speaker model) couldn't recover speaker structure on URTIC — KMeans-vs-HDBSCAN ARI = 0.093. This rules out architectural circularity (the speaker structure isn't an artefact of clustering ECAPA representations *and then* probing with ECAPA-derived structure).

**The speaker probe.** Given any feature vector for an utterance, train a model to predict its pseudo-speaker ID (210-class). On `devel_val` the probe sees chunks from speakers it never saw during fitting (since `train` and `devel` are speaker-disjoint), so high top-1 accuracy means the *features themselves* carry generalisable speaker identity — not just memorised mappings.

- **Chance** = 1/210 = 0.00476
- **A2's WavLM features (deep MLP probe, `speakers/probe.py`)** → top-1 = 0.0501 (~10× chance) — the headline A2 number reported in `results/A2.json`. Trained ~30 epochs.
- **A2's WavLM features (multinomial LR probe, `honesty/probe.py`)** → top-1 = 0.0674 random splits, **0.0760 grouped splits** — what every A5a/A5b/A5d audit measures. LR with L2 extracts more speaker information from a 4096-d substrate than the MLP probe recovers under its training schedule, so the LR number is **the apples-to-apples ceiling** for any audit comparison.
- **A3 manner-pooled features (deep MLP probe)** → top-1 = 0.0555 — *more* leakage than A2 under the same probe, hence rejected.

**Why the grouped-split probe number is HIGHER than the random-split number** (and why that's actually *better* honesty measurement, not worse). Under random splits, `train_fit` and `devel_val` share 198/210 pseudo-speakers — so a probe trained on `train_fit` z literally has seen those speakers' voices during training, and `top-1 = 0.067` is partly *recognition* of voices it has been trained on. Under grouped splits, train_fit and devel_val share **0/210** pseudo-speakers — a probe's `top-1 = 0.076` measures genuine *generalisation across unseen pseudo-speakers*. Same probe code, different generalisation regimes. The grouped-split number is the strictly more honest measurement of "how much speaker information do these features carry, in a way that transfers to new speakers?" — and that's the right question for a paper about speaker-confound-aware audits. Both are reported, but the grouped-split LR ceiling (0.0780) is the one A5b/A5d gates anchor on going forward.

A speaker probe trained on the *same* features as the cold classifier tells you how much of the cold model's prediction could plausibly be speaker shortcut. Within an audit family, always compare numbers from the *same* probe substrate AND the *same* split protocol.

---

## 7. The path so far

### A1 (folded into A2)
The original PDF plan started with mean-pool over WavLM frames. Mean-pool is a strict subset of layer-weighted pooled-stats, so we skipped it.

### A2 — locked, UAR 0.6428 ± 0.003 (random splits, uniform layer-weight init — historical)
Frozen WavLM-Large, 25 layers, per-layer pooled stats (mean + std + skew + kurt over the time axis = 4 × 1024 = 4096-d per layer). Then:
1. **FeatureStandardiser** — z-score each of the 25 × 4096 features on `train_fit`. Critical: raw pooled stats span ~4 orders of magnitude in std, which collapses the head to majority-class.
2. **Softmax layer weights** — learn 25 weights, softmax-normalised; sum the layers into one 4096-d vector.
3. **2-layer MLP** (4096 → 128 → 128 → 2 logits) with BatchNorm, GELU, dropout 0.5.
4. Balanced sampler in the DataLoader (each batch sees roughly equal cold and non-cold).

This is the **historical anchor**. β_A2 = 1 in the original A5b fusion lock. Subsumed by A2.5 (below) as the active baseline going forward.

### A2.5 — NEW canonical baseline, UAR 0.6564 ± 0.0038 (grouped splits, honesty-prior init)
Same architecture and training recipe as A2 — only the layer-weight softmax init changes. Initialised with `head.layer_weights ← T_INV * sub@1` from [results/A5d_grouped_layer_honesty.csv](results/A5d_grouped_layer_honesty.csv), T_INV=50 (gives prior softmax max/min ratio ~8.5×).

**Result: +0.020 UAR (4.7σ) over uniform-init A2_grouped, with no probe inflation** — MLP probe flat (0.0501 → 0.0501), LR probe slightly down (0.0760 → 0.0725). Pareto improvement on every measured dimension.

**The mechanistic finding** (paper claim): the optimizer **does not move the prior at all** — `cos(init_softmax, final_softmax) = 0.9996–0.9998` across all 3 seeds, max delta ≈ 0.003. This settles why uniform-init A2 always converged to uniform: the joint loss landscape is flat in the layer-weight direction (the head's MLP can solve cold prediction to its asymptote regardless of layer mix), so without gradient pressure to specialise, whatever you initialise with becomes the equilibrium. **For foundation-model layer weighting in low-data settings, "let the optimizer figure it out" can fail silently** — data-derived priors do real work, not vanity init.

**Layer selection** (top-5 final softmax weights consistent across seeds): `[0, 2, 5, 22, 6]` ≈ prior's `[0, 2, 5, 22, 24]`. Notable mix: early speaker-rich layers (L0/L2/L5 — high cold UAR but also high speaker top-1) plus late cold-pure layers (L22/L24 — lower cold UAR but lowest speaker top-1). The downstream per-channel cold-MLP handles each via different paths: speaker info correlated with cold gets retained, speaker info orthogonal to cold gets stripped.

**Open concern: val-test gap widens.** -0.0133 → -0.0202 (1.5σ shift, more pessimistic). Likely from earlier early-stopping (epochs 2-3 vs 3-7); worth re-checking at 5 seeds before paper submission.

This is the **new anchor** for everything downstream — A5.5, A6, A7. A5b K=1 will be re-evaluated against it before the paper-headline number is locked.

### A3 — null result, rejected
Tried two ideas:
1. **Phoneme-CTC labels** via `wav2vec2-xlsr-53-espeak-cv-ft`. 84% of frames came back as `<blank>` because the model was trained on CommonVoice (clean read speech, IPA targets) and URTIC is recording-booth German with different phoneme inventory. **Documented as abandoned negative result.**
2. **Manner labels** (silence / voiced / unvoiced) from pYIN + RMS. Labels themselves are clean, but the *head* — pool WavLM features per category, concat into 6144-d — failed both gates: argmax UAR −0.008 vs A2, speaker-probe top1 +0.005 vs A2. The high-dim concat let an MLP rediscover speaker shortcuts. **Rejected as a standalone stream.** Manner labels survived as conditioning information for A5 features.

### A5 — current rung

Where we are now. Sub-rungs in execution order:

- **A5a** — *honesty audit* (done, 8 groups). Build per-group features, audit each one for cold predictivity *and* speaker leakage.
- **A5b** — *constrained late fusion* (**PASS at K=1**). K-locked K=1 (`A2 + G4_gain_invariant`): UAR **0.6576 ± 0.0011** vs A2 0.6428 ± 0.0034 — Δ +0.0148 ± 0.0045 (3.3σ above zero, gate of +0.007 cleared by ~2σ); Δ vs A2_τ +0.0112 ± 0.0066 (fusion ≈75% of lift, τ ≈25%). The original free K∈{1..4} sweep produced 0.6502 ± 0.0078 with σ > effect size — documented as τ-sweep pathology (free K-sweep over-rewards configs with more τ flexibility); σ collapse 0.0112 → 0.0011 between free-sweep and K-locked is the diagnostic.
- **A5d** — *per-layer honesty diagnostic* (**DONE → A5e SKIPPED + structural finding**). For each WavLM layer L ∈ [0, 24], cold + speaker probes on the cached `pooled[:, L, :]`. Result: best `sub@1` L21 = +0.0387 (≪ 0.15 trigger); best `cold_uar` L7 = 0.6052 with `speaker_top1` 0.0813 at the same band. Speaker top-1 decays monotonically L0→L24 (0.087→0.043, confirms Pasad 2021 / Chen 2022 for speaker), but cold UAR is **flat** across the stack (range 0.56–0.61) — refutes mid-band cold hypothesis on URTIC.
- **A5e** — *WavLM mid-layer retrain* (**SKIPPED**). A5d closed the retrain track (no honest mid-band). GPU goes to A5.5 / A6 instead.
- **A5c** — *learned gate* (revivable). Was scoped to fire after A5b passed; A5b passes at K=1, but a single-group fusion leaves little for a learned residual to refine over. On hold pending A5.5 / A6 outcomes.

---

## 8. Feature groups (the meat of A5)

For each group `g` we measure:
- `UAR_g` — how well a linear logistic regression on group `g` alone predicts cold on `devel_val`
- `top1_g` — how well the same linear architecture predicts pseudo-speaker on `devel_val`
- `label_gain_g = UAR_g − 0.5` (UAR over chance)
- `speaker_gain_g = top1_g − 1/210` (top-1 over chance)
- `subtractive_honesty_g = label_gain_g − λ · speaker_gain_g` (default λ = 1)

A group gets admitted into A5b if `subtractive_honesty > 0` and `label_gain > 0`. Linear-only by design — if a group needs nonlinearity to predict cold, that's a signal the group should be sub-divided rather than blessed with hidden capacity (which is exactly how A3 found speaker shortcuts).

### G1 — Voicing scalars (free, ~9 dims)

**What:** Statistics computed over the cached manner labels (silence / voiced / unvoiced per frame at 50 Hz).

**Features:** `voiced_fraction`, `unvoiced_fraction`, `silence_fraction`, `voicing_dropout_per_sec` (count of voiced→unvoiced transitions), mean segment lengths for each category, `long_silence_rate_per_sec` (silence runs ≥ 200 ms).

**Physical meaning:** A cold subject often has more pauses (sniffling, throat-clearing breaks) and more voicing dropouts (vocal folds fail to maintain a stable vibration, dropping into a breathy or unvoiced region mid-syllable). Speech rhythm is altered.

**Extraction:** Pure post-processing over `cache/manner_labels/*.pt`. Zero waveform reads. Free.

**A5a result:** UAR 0.5831, label_gain +0.0831. Speaker leak very low (top1 0.011, ~2× chance). **Highest honesty ratio (11.4)** — the cleanest signal in the audit. Cold-biased: recall_C = 0.642 > recall_NC = 0.524, so it's pulling toward predicting cold on the margin (useful complement to A2 which under-recalls cold).

### G2 — F0 / prosody (~10 dims)

**What:** Per-utterance statistics of the F0 contour.

**Features:** `f0_mean_hz`, `f0_std_hz`, percentiles (p10, p90), `f0_range_hz` (p90−p10, robust spread), `f0_log_mean_st` (mean in semitones-from-100Hz, perceptually meaningful), `f0_jitter_local` (mean |ΔF0| / mean F0), `f0_voiced_fraction`, `f0_missingness_in_voiced` (frames the manner labeller called voiced but pYIN couldn't track F0 — a sanity / quality check), `f0_voiced_run_count_per_sec`.

**Physical meaning:** Cold can *raise* F0 (vocal-fold inflammation makes the folds stiffer → higher pitch) or *lower* it (mucus loading → heavier folds → lower pitch). F0 *variability* increases — pitch-tracking algorithms see more wobble. Voiced run length shortens.

**Extraction:** Two-step. First, run `librosa.pyin` (probabilistic YIN — a pitch tracker) on each waveform with the same hop length (320 samples = 20 ms) as the manner labels, so frame indices align. Save the F0 contour to `cache/f0/{stem}.npy` (NaN where unvoiced). Second, derive scalars from the cached contour. Total CPU: ~11.5 hours one-time on the full corpus.

**A5a result:** UAR 0.5680, label_gain +0.0680. Higher speaker leak than G1 (top1 0.0194, ~4× chance — F0 distribution is somewhat speaker-identifying), but `subtractive@1 = +0.053 > 0` so it passes admission. Also cold-biased.

### G3 — Voice quality (~14 dims)

**What:** The classical clinical voice-quality measures, computed by openSMILE.

**Features:** `jitterLocal`, `shimmerLocaldB`, `HNRdBACF` (mean + std of each), and four spectral-tilt measures restricted to voiced frames: `alphaRatioV`, `hammarbergIndexV`, `slopeV0-500`, `slopeV500-1500`. All are eGeMAPSv02 functionals.

**Physical meaning:** This is the *textbook* feature group for voice pathology. Cold inflames the vocal folds, increases mucus, alters airflow. The result: jitter and shimmer rise (more cycle-to-cycle perturbation), HNR drops (more noise relative to harmonic structure), spectral tilt flattens (voice gets pressed). Schuller 2017, Cummins 2017, Huckvale 2018 all flag this as the most physiologically motivated group.

**Extraction:** `opensmile-python` running the eGeMAPSv02 functionals config — 88 features per utterance. Cache once at `cache/handcrafted/egemaps/{stem}.npy` plus column-name list at `_columns.json`. We then *carve* the 14 voice-quality columns by name prefix. Carving is a runtime slice, not a re-extraction — if we change which columns count as G3 we don't need to re-run openSMILE.

**A5a result:** UAR 0.5591, label_gain +0.0591 (the weakest passing group). Speaker leak is moderate (top1 0.0233, ~5× chance). NC-biased: recall_NC = 0.611 > recall_C = 0.507. Honesty `subtractive@1 = +0.041 > 0` — admitted, but on the margin.

### G4 — Energy / pause / breath (~11 dims)

**What:** RMS-based loudness statistics, both global and conditioned on manner regime.

**Features (cols 0–3, absolute amplitude):** `rms_lin_mean`, `rms_lin_std`, `rms_db_mean`, `rms_db_std`. **(cols 4–10, gain-invariant):** `low_energy_ratio` (fraction of frames in the bottom 10% of RMS), `energy_slope_db_per_sec` (linear fit of dB-vs-time), three regime-contrast scalars (`rms_db_voiced − rms_db_silence`, etc. — these *cancel* any constant gain offset), `long_pause_per_sec`, `median_silence_seg_sec`.

**Physical meaning:** Two distinct signals. (1) Absolute amplitude — a cold patient may speak more quietly, or louder due to compensation. (2) Energy *contrast* between voiced and silence — illness changes how dramatically a speaker modulates loudness.

**Extraction:** One-shot RMS pass over the waveform. Cached per-stem at `cache/handcrafted/g4/{stem}.npy`. Tens of minutes total.

**A5a result:** UAR 0.6418, label_gain +0.1418 — **the strongest single group.**

**The amplitude-confound concern, and what we did about it.** Cols 0–3 (raw absolute amplitude) raise a red flag: if recordings are made with different mic gain across speakers or sessions, "louder = cold" might really be "louder = certain mic". To check, we ran a **gain-invariant ablation**: drop cols 0–3, keep cols 4–10. New UAR 0.6318 (only 0.010 drop) and speaker_gain halves from +0.013 to +0.008. Verdict: G4 is *genuinely physiological*, not a recording-gain artefact. The cleaner slice (`G4_gain_invariant`) is what we admit into A5b — same essential signal, less confound.

### G6 — Spectral shape (~21 dims)

**What:** Low-order MFCCs and spectral flux, in three regime variants.

**Features:** `spectralFlux_*` (no-suffix = whole signal, V = voiced only, UV = unvoiced only), `mfcc1` through `mfcc4` (mean and std-norm of each), and the same three regime variants. Total 21 columns from eGeMAPSv02.

**Physical meaning:** MFCCs describe the broad shape of the spectrum. Cold thickens secretions on the vocal-tract walls and changes nasal coupling, both of which subtly shift the spectral envelope. Spectral flux measures articulation rate — how quickly the spectrum changes frame-to-frame; cold often slows articulation.

**Caveat:** Low-order MFCCs are also classical *speaker* features. They encode vocal-tract shape (how long your vocal tract is, how your tongue resting position curves the resonances). So we expect this group to leak speaker information, and the audit confirms it: speaker_gain = +0.029, the highest of any predictive group (~7× chance top-1). Still, label_gain = +0.105 is the second-strongest, and `subtractive@1 = +0.076 > 0`, so it passes admission at λ=1. It would *fail* admission at λ=2 (= +0.047, still positive, but margin thin).

**Extraction:** Same eGeMAPSv02 cache as G3, just a different name-prefix carve.

**A5a result:** UAR 0.6050, label_gain +0.105. Honesty `subtractive@1 = +0.076`. NC-biased.

### G8 — OOD Mahalanobis (1 dim)

**What:** *One scalar per utterance* — the squared Mahalanobis distance from the utterance's A2-fused vector to the centroid of the non-cold training distribution.

**Idea:** The original A5 (in the PDF plan) was *just* this feature. The reasoning: a model trained mostly on healthy speech learns a "normal" manifold; cold speech is an outlier. Distance from the manifold should correlate with cold.

**Extraction:**
1. Load the trained A2 head.
2. Run forward up to (but not through) the classifier — give us 4096-d fused vectors per utterance.
3. Fit a **LedoitWolf shrinkage covariance** on `train_fit` non-cold vectors (LedoitWolf handles the rank-deficient covariance you get when N < d, common when "non-cold subset of train_fit" is not huge).
4. For each test utterance, compute `(x − μ)ᵀ Σ⁻¹ (x − μ)` — the Mahalanobis distance. That's the feature.

**A5a result:** **UAR 0.4334 — anti-predictive!** label_gain = −0.067. The distance is *lower* for cold cases on average (cold lives near the middle of the non-cold manifold, in the dimensions A2 cares about). This is a *documented negative result* — we drop G8 from the admission pool but keep the row in the table because "OOD as a cold signal" was the original A5 hypothesis and it's important to publish that it doesn't hold here.

### G5 — Modulation spectrogram (64 dims)

**What:** The Huckvale-2018-style **MOD** family — per-mel-band amplitude-modulation rates aggregated to a small fixed-size descriptor. This was the one feature family from the 2017 challenge literature we hadn't covered with G1-G4 / G6, and it captures something genuinely orthogonal: cross-frame *dynamics*, not per-frame *statics*.

**Features:** 4 acoustic super-bands × 8 modulation bands × {mean, std} = 64.
- Acoustic super-bands: split the 40-band log-mel spectrum into low / mid-low / mid-high / high (10 mels per band).
- Modulation bands: 8 log-spaced bins from 1 Hz to 20 Hz — covers slow envelope (<2 Hz), syllable rate (3-8 Hz), and fine perturbation (10-20 Hz) regimes.
- For each (super-band, modulation-band) cell, take the magnitude of the per-mel-band-FFT-over-time within that frequency bin and report (mean, std) over the mel rows in the super-band.

**Physical meaning:** Cold disturbs amplitude modulation at all three of those rate scales — congestion slows syllable rate (3-8 Hz energy redistributes), breathiness changes phrase pacing (<2 Hz), vocal-fold inflammation perturbs the higher rates. None of G1-G4/G6 see across more than a handful of adjacent frames. G5 is the first group that summarises *full-utterance temporal envelope structure* in the frequency domain.

**Extraction:** For each utterance, compute the log-mel spectrogram (40 bands at 100 Hz frame rate, hop=160 at sr=16k). Subtract per-band mean (remove DC). Hann-window in time. rFFT along time → magnitude. Bin and aggregate. Cached at `cache/handcrafted/modulation/{stem}.npy` as 64-d fp32. ~5 minutes CPU on the full corpus.

**Why aggressive aggregation (64-d, not the full per-band spectrum):** the linear-only probe discipline in plan.md § 5.6 says if a group needs nonlinearity to predict cold it should be sub-divided rather than blessed with hidden capacity. A 40-mel × 16-bin per-band spectrum (640+ dims) gives a linear LR enough surface area to launder speaker identity in the same way the A3 head did. Aggregating to 64-d before the probe sees it forces the predictive signal to live in *low-rank* modulation structure, which is what the literature claims is cold-relevant.

**A5a result:** UAR 0.5802, label_gain +0.0802, speaker_top1 0.0146 (~3× chance — much cleaner than G3 / G6), ratio 7.40, `sub@1 = +0.0703`. **Lands at #4 in admission ordering**, just 0.005 below G6_spectral_shape (+0.0758). The A5b sweep window was extended from K∈{1,2,3} to K∈{1,2,3,4} after this audit landed — G5 enters as the fourth admitted group, and K=4 wins the val sweep on 2/3 seeds. The MOD family being honest *and* validating the literature pointer (Huckvale's 2018 reanalysis) without being redundantly correlated with A2 is the load-bearing finding here.

### G7 — not implemented in v1

**G7** (regime-conditioned mel-band stats, ~160 dims) was flagged in plan.md § 5.2 as the highest-risk group — it's structurally close to a vocal-tract envelope, the same fingerprint that crashed A3. Held for a stricter acceptance protocol (`label_gain ≥ 0.05` and one-shot eval on `devel_test` before admission). Not run for v1.

---

## 9. The A5a honesty audit, the actual numbers

Snapshot of `results/A5a_honesty.csv`, ordered by `sub@1` descending (= admission ranking):

```text
group                    dim     UAR   lab_gain   spk_top1   spk_gain    ratio     sub@1
G4_energy                 11  0.6418    +0.1418     0.0181    +0.0134    +9.87   +0.1284
G4_gain_invariant          7  0.6318    +0.1318     0.0127    +0.0080   +14.73   +0.1239
G1_voicing                 9  0.5831    +0.0831     0.0110    +0.0063   +11.41   +0.0768
G6_spectral_shape         21  0.6050    +0.1050     0.0340    +0.0292    +3.48   +0.0758
G5_modulation             64  0.5802    +0.0802     0.0146    +0.0098    +7.40   +0.0703
G2_prosody                10  0.5680    +0.0680     0.0194    +0.0146    +4.35   +0.0534
G3_voice_quality          14  0.5591    +0.0591     0.0233    +0.0186    +3.02   +0.0405
G8_ood_mahalanobis         1  0.4334    -0.0666     0.0073    +0.0025   -18.86   -0.0692
```

**Reading this table:**
- **`UAR`** — cold-probe UAR on `devel_val`. Chance = 0.5.
- **`lab_gain`** = UAR − 0.5. How much the group lifts cold prediction over chance.
- **`spk_top1`** — speaker-probe top-1 accuracy on `devel_val`. Chance = 1/210 = 0.0048.
- **`spk_gain`** = top1 − 1/210. How much the group lifts speaker prediction over chance.
- **`ratio`** = lab_gain / (spk_gain + 1e-3). Honest if >> 0; reports cold lift per unit speaker lift. Parameter-free.
- **`sub@1`** = lab_gain − 1·spk_gain. The subtractive form at λ=1. The admission key for A5b.

**Per-group take:**
- **G4_energy / G4_gain_invariant** are the strongest groups. They're the same waveform information so we can only admit one (admitting both would double-count). The gain-invariant slice keeps almost all the predictive power (UAR drop 0.010) with a much cleaner honesty story (ratio 14.7 vs 9.9, spk_gain halved). **Admitted: G4_gain_invariant.**
- **G1_voicing** is the most honest signal in the audit (ratio 11.4). Modest predictive lift but speaker-clean. Lands at #2 in the admission ordering.
- **G6_spectral_shape** is a strong predictor (#3 by sub@1) but the highest speaker leaker. MFCCs being speaker-rich is a known property — that's why we measured the leak rather than trusting the predictive lift naïvely.
- **G5_modulation** lands at #4, only 0.005 sub@1 below G6. Honest (ratio 7.4, low speaker leak) and validates the literature pointer (Huckvale's MOD family). The A5b sweep window was extended to K∈{1,2,3,4} so G5 can compete; K=4 wins the val sweep on 2/3 seeds.
- **G2_prosody** and **G3_voice_quality** both pass admission at λ=1 but on the margin.
- **G8_ood** is anti-predictive — *documented* and *dropped*.

---

## 10. Late fusion (A5b)

**Formula:**
```text
final_logit = logit_A2 + β · mean_g( zscore_g( logit_g ) )
```
- `logit_A2` is the binary log-odds from A2's classifier. Frozen, β_A2 = 1.
- `logit_g` is `clf.decision_function(scaler.transform(X_g))` from the per-group cold probe. Frozen at the values audited in A5a.
- `zscore_g` removes per-group scale differences (G4's logit naturally spans a wider range than G2's). Mean and std are fit on `train_fit` so β has the same interpretation across groups.
- `mean_g` averages over the **K admitted groups** (the top-K by `sub@1`).
- `β` is a single scalar — no per-group βs, no learned parameters. Hard top-K admission.

**Sweep:**

- `β ∈ {0.25, 0.5, 1.0}`
- `K ∈ {1, 2, 3, 4}` — admission order by `sub@1`: G4_gain_invariant → G1_voicing → G6_spectral_shape → G5_modulation. The window was extended from {1, 2, 3} to {1, 2, 3, 4} after A5a placed G5 just 0.005 sub@1 below G6 — outside the original window but a clean honest signal worth letting compete.
- **τ** (decision threshold on the fused logit) — also swept on `train_threshold`.

All sweeping is on `train_threshold`. The locked `(β*, K*, τ*)` is evaluated **once** on `devel_test`. No tuning on devel. Three training seeds {42, 123, 7} for variance estimation.

**Acceptance gate** (plan.md § 5.7): A5b mean UAR on `devel_test` ≥ A2 mean + 0.007 (= 2σ at N=3, so any improvement smaller than this is statistical noise).

**Locked result (gate PASS at K=1).** With admission frozen to the top-1 group (`A2 + G4_gain_invariant`) and only β and τ swept on `train_threshold`, A5b devel_test UAR = **0.6576 ± 0.0011** (argmax on the fused logit) vs A2 0.6428 ± 0.0034 → **Δ +0.0148 ± 0.0045** (3.3σ above zero, gate of +0.007 cleared by ~2σ). Per-seed: 42→0.6571, 123→0.6589, 7→0.6569. Calibration-aware Δ vs A2_τ = +0.0112 ± 0.0066 (1.7σ — fusion contributes ~75% of the headline lift; τ-tuning the A2 logits alone contributes the remaining ~25%). Fused-vector speaker probe top-1 = 0.0194 on `[logit_A2, z_logit_G4_gi]`, well below the 0.0510 ceiling.

**Documented τ-sweep pathology (free K∈{1..4} sweep).** The originally-locked free-sweep result was UAR 0.6502 ± 0.0078, Δ +0.0074 ± 0.0112 — the mean hit +0.007 but per-seed σ exceeded the effect size, so fusion was not reliably winning. Per-seed K winners were {42→K=4, 123→K=4, 7→K=2}. The diagnosis: **free K-sweep on `train_threshold` over-rewards configurations with more τ flexibility** (more groups widen the achievable τ-vs-UAR curve), inflating variance without improving the mean. The σ collapse from 0.0112 (free K-sweep) to 0.0011 (K-locked K=1) is the diagnostic — variance drops 7× when admission is frozen. Both numbers are paper-reportable: K=1 is the headline, K=4 is the documented sweep-protocol finding.

**Paper framing.** A5b passes with a *selective single-group fusion*: WavLM A2 logits plus gain-invariant energy/pause features (G4_gi). Broader handcrafted fusion did not help despite positive A5a honesty scores — the §10.1 diagnostics show that the additional groups (G6, G5, G1) carry signal that overlaps Pearson 0.30–0.63 with A2 or with G4. The mechanism is **redundancy with A2**, not absence of signal in the other groups: G6 has +0.105 standalone label_gain, but ~0.20 of that is already in A2's logit (Pearson² over 0.45 correlation), and G1 is 63% redundant with G4. The useful handcrafted-and-A2-orthogonal cold signal concentrates in **temporal energy structure** (G4 = energy + pause + breath statistics, all with gain-invariant variants); broader fusion adds groups whose lift is largely already in A2.

**Locked-K speaker probe (DONE; both gates PASS).** The fused-vector probe in `results/A5b_diag.json` (top-1 = 0.0194) was on the *full admission pool* concat — `[logit_A2, z_logit_g for g in {G4, G1, G6, G5}]` — not specifically the K=1 representation. The locked-K cell ships two probes plus capacity controls in `A5b.json::locked_speaker_probe` and `…::locked_speaker_probe_controls`:

```text
                                       top-1 (3 seeds)        notes
(i)  literal 2-D  [logit_A2, z_g4]      0.0119 ± 0.0015        fusion-input view
(ii) backbone concat [pooled_4096, g4]  0.0675 ± 0.0006        leak-channel audit
(a)  pooled-only  [pooled_4096]         0.0674 ± 0.0006        codepath-consistent A2 ref
(b)  pooled + 7-d Gaussian noise        0.0665 ± 0.0026        capacity sanity
ceiling = (a) + 1σ                      0.0680                 honesty.speaker_probe
```

Verdict: **probe (i) PASS** (0.0119 ≪ 0.0680, ~6× margin) and **probe (ii) PASS** (0.0675 ≤ 0.0680, by 0.0005). The G4_gi addition contributes +0.0001 to speaker recoverability above pooled-alone — essentially zero, no leak channel.

**Probe-substrate clarification.** A5a/A5b/A5d all use `honesty.speaker_probe` (multinomial LR with L2). The historical 0.0501 in `results/A2.json::speaker_probe` is from `speakers/probe.py`'s deeper MLP probe trained ~30 epochs — a different substrate that gives different numbers on the same A2 fused vector. The **0.0674 ± 0.0006** under `honesty.speaker_probe` is the apples-to-apples A2 reference for any audit anchored on that codepath; ceiling 0.0680 is the right gate for the locked-K probes. The earlier reading of "+0.017 lift in (ii) over A2" was a substrate mismatch (LR (ii) vs MLP (A2)), not real interaction information.

**Architectural reading.** Probe (i) 0.0119 vs probe (ii) 0.0675 = **~5.5 pp speaker-info drop** between the upstream concat and what the fusion classifier actually sees. The per-channel cold probe is a speaker-information bottleneck *by design*: each channel is compressed to a single scalar logit tuned for cold prediction, which strips the speaker-side variance present in the upstream pooled vector. This is the clean architectural justification for choosing logit-level fusion over A3's concat-MLP — speaker information exists upstream but never reaches the fusion classifier. The capacity control (b) confirms the LR's L2 regularises 7 noise dims out cleanly, so the +0.0001 (ii)−(a) gap is real (just very small), not a sklearn artefact.

**Why this exact formula?**
- *Logit-level* — not concat-then-MLP. Concat-MLP is the substrate that let A3 rediscover speaker shortcuts. Forcing each group through its own 1-d cold-probe logit *before* fusion means every group has to prove standalone cold utility before it gets any β weight.
- *No learned weights* — the table itself is the selection mechanism. Cleaner paper story ("honesty-weighted fusion with no learning at the fusion stage lifts UAR by Δ") than "regularised βs."
- *Hard top-K rather than soft weighted by honesty* — sharper claim, cleaner ablation. If A5c (learned gate) lifts further, the delta is unambiguously the gate.

### 10.1 Diagnostics that ship next to A5b (do not change locked numbers)

Three short cells run after the A5b sweep. They feed paper figures and tell us *where* the gate fails, none touches `(β*, K*, τ*)`.

**(a) Logit correlations + argmax disagreement vs A2.** The honesty audit ranks groups in isolation. It cannot tell you whether two admitted groups are pulling in the same direction (in which case `mean_g(z_g)` over both is just one weighted vote repeated twice — wasted budget) or whether they catch different errors (genuine complementarity). We compute:

- A Pearson matrix on `devel_val` over `{logit_A2, z_logit_g for g in admission_pool}` — the diagonal of redundancy with the A2 anchor (right-most column) and pairwise redundancy across groups (off-diagonal block).
- Per-group **argmax disagreement** = fraction of utterances where `sign(logit_A2) ≠ sign(z_logit_g)`. This is a non-Pearson, ranking-style view that survives monotonic nonlinear relationships (e.g. `z_g = sigmoid(logit_A2 + noise)` would correlate strongly with A2 in Pearson but still disagree at the threshold). Higher disagreement = more independent error patterns.

What this answers: a group with high `sub@1` (admitted) but high A2-correlation (Pearson ~0.6+) and low disagreement (<0.2) is mostly re-saying what A2 already says — its admission is a less effective lever than the table suggests. We expected this for G6 (low MFCCs and WavLM both encode spectral envelope); we already saw it *not* be the case for G4 in earlier checks (Pearson ~0.4, argmax disagreement ~0.35 — real complementarity).

**(b) Fused-vector speaker probe.** plan.md § 5.5 says "probe top-1 on A5b representation ≤ A2 + 1σ ≤ 0.0510." Literally probing the 1-d fused logit against 210 pseudo-speaker classes is degenerate (multinomial LR over one feature can't separate that many classes). The honest version probes the actual `[logit_A2, z_logit_g for g in admitted]` vector — what fusion has access to, dimensionality 1 + K. We report top-1 on `devel_val` for two cuts:

- **Full admission pool** — sanity ceiling. If this is fine, every K we'd consider is fine.
- **Locked top-K admitted** (read from `results/A5b.json`) — the actual gate to report.

Comparison reference: max per-group `spk_top1` from the A5a CSV. If the concat top-1 stays at or below that ceiling, no group combination is creating a *new* speaker channel beyond what its parts already carry. If it spikes (say to 0.07+ vs A2's 0.05 reference), one of the admitted groups is amplifying speaker info when seen *jointly* with another — admission would need to be revisited even though every individual group passed.

**(c) Redundancy-adjusted ranking (paper diagnostic).** The A5a `sub@1` admission key sees each group in isolation; it doesn't know how much each group's signal already overlaps with A2. Per group on `devel_val`:

```text
unique_gain      = label_gain · (1 − corr(logit_A2, z_logit_g)²)
safe_unique_gain = unique_gain − speaker_gain
```

The first term is the cold lift the group adds *beyond* what A2 already explains (Pearson² is the fraction of variance shared with A2; `(1 − r²)` is the residual). The second penalises that residual lift by speaker leak. Diagnostic only — admission still runs on `sub@1` so that the criterion can't be retroactively reshaped to fit the result. The redundancy-adjusted column is the paper's "what does each group *uniquely* contribute over A2?" answer.

Single seed (42) — structural, not a multi-seed UAR claim. Output: `results/A5b_diag.json` (correlations + fused-vector probe + redundancy-adjusted table).

**What the diagnostics found.** Three results that change how A5b reads:

- **G4 ↔ G1 are nearly the same group (Pearson 0.628).** Both built off the manner labels (G1 = voicing fractions, G4 = energy conditioned on manner regimes). The K=2 winner under raw `sub@1` is `A2 + G4 + G1`, but G1 contributes much less than the table suggests because most of its predictive signal is already in G4.
- **G5 jumps over G6 in the redundancy-adjusted ranking.** Raw `sub@1` ranks G6 #3 (+0.0758) above G5 #4 (+0.0703); after `safe_unique = label_gain·(1−r²) − speaker_gain`, G5 (+0.0630) lands #3 ahead of G6 (+0.0543). G6's 0.45 correlation with A2 means a fifth of its label_gain is already in A2's logit. Empirical post-hoc support for the K=4 sweep extension.
- **No hidden speaker leak in the fusion.** Fused-vector speaker probe top-1 = 0.0194 on the admitted concat — *below* the per-group ceiling (G6 = 0.0340) and an order of magnitude below A2's 0.0501. Combining admitted groups does not create a speaker channel beyond what the worst single group carries.

**The headline diagnosis: the gate FAIL is signal redundancy, not speaker leak.** Three of four K=4 admits (G4, G6, G5) correlate 0.30–0.45 with A2; one (G1) is 63% redundant with another admit (G4). "Mean over 4 groups" averages fewer independent voters than 4, and the resulting variance dominates the +0.0074 mean lift. The K=2 ablation is now the cleaner near-term read — if `A2+G4+G5` beats `A2+G4+G1` on devel_test, redundancy is confirmed as the lever and the paper claim shifts from "fusion works" to "fusion works **once you adjust for A2 overlap**."

### 10.2 Calibration-aware baseline and K-locked ablation

After A5b's free-K-sweep three-seed run, one extra cell reports two things needed to interpret the result, and turns out to be **what flipped the gate from FAIL to PASS**:

- **A2_τ baseline.** Vanilla A2 (§7) is **argmax** (τ=0). A5b sweeps τ. To rule out "the lift is just threshold movement, not fusion," we sweep τ on A2's logits on `train_threshold` and lock once on `devel_test`. Δ vs A2_τ tells us how much of the A5b lift is fusion versus calibration.
- **K-locked ablation.** Instead of sweeping K, lock admission to one of {K=1: A2+G4_gi}, {K=2: A2+G4+G1}, {K=2: A2+G4+G5}, {K=2: A2+G4+G6} and sweep only β, τ. Removes the K-sweep degree of freedom that the original free K∈{1..4} sweep was over-fitting on `train_threshold`.

**Three-seed ablation result (`results/A5b_ablation.json`):**

```text
config               devel_test UAR    Δ vs A2_argmax       Δ vs A2_τ
A2 (argmax)          0.6428 ± 0.0034   +0.0000              -0.0036 ± 0.0109
A2 (τ-tuned)         0.6464 ± 0.0075   +0.0036 ± 0.0109     +0.0000
A2 + G4_gi  (K=1)    0.6576 ± 0.0011   +0.0148 ± 0.0045 *   +0.0112 ± 0.0066
A2 + G4 + G1 (K=2)   0.6532 ± 0.0059   +0.0104 ± 0.0091     +0.0068 ± 0.0027
A2 + G4 + G5 (K=2)   0.6512 ± 0.0044   +0.0084 ± 0.0069     +0.0049 ± 0.0068
A2 + G4 + G6 (K=2)   0.6519 ± 0.0038   +0.0091 ± 0.0060     +0.0055 ± 0.0075
A2 + 4 grps  (K=4)   0.6502 ± 0.0078   +0.0074 ± 0.0112     +0.0039 ± ...
```

Reading: the K=1 lock (`A2 + G4_gain_invariant` only) is the cleanest configuration on every axis — highest UAR, *lowest* per-seed σ (0.0011, 7× tighter than the K=4 free sweep), highest mean Δ over both A2_argmax and A2_τ. Adding any second group (G1, G5, or G6) raises σ and lowers the mean — adding G1 specifically loses the most because of the G4↔G1 0.628 redundancy diagnosed in §10.1. **Verdict: A5b passes at K=1; the K=4 free-sweep result was protocol over-fit on the K dimension.**

Per-seed K=1 result confirms the lock is not dragging on a single seed: 42→0.6571, 123→0.6589, 7→0.6569.

---

## 11. What's next (post-A5b PASS, post-A5d run — A5.5 / A6 / A7; A5e SKIPPED)

A5b's K=1 PASS rewrote the next-rung order. The original sequence (A5d → A5e → A5.5/A6/A7) was scoped on the assumption that A5b would FAIL and we'd need a structural rescue (band-restricted backbone). A5b passes via a much smaller intervention — a single audited group added on top of the existing A2 backbone — so the rescue route is no longer load-bearing. **A5d has now run and closed the retrain track**: both branches of the A5e trigger condition fired simultaneously (no `sub@1_L > 0.15`; cold-UAR peak coincides with speaker peak). A5e is **SKIPPED**. The de-confounding ladder (A5.5 → A6 → A7) moves up.

### A5.5 — cross-speaker augmentation (LOCKED at conservative-α embedding mixup; α-axis closed)

Originally scoped as audio splicing (killed by Phase 3.5 self-splice control, M8). Pivoted to **embedding mixup** on cached WavLM pooled stats: `α · anchor + (1-α) · partner`, partner = same Cold + different pseudo-speaker. **LOCKED at conservative α ∈ [0.70, 0.85]** (Plan B Phase 4: UAR Δ +0.006 vs A2.5, recall pattern more cold-balanced, both speaker probes within noise) as the data-level rung. Aggressive α ∈ [0.50, 0.70] sweep ran as a clarifying experiment and returned **branch (d): UAR DROPS (Δ -0.017, σ 6×) AND probe still doesn't drop** — the cleanest possible disposition for locking, because it closes the α-axis (both endpoints fail differently; intermediate ranges can't escape) rather than leaving "try another α" open. Ships as the **ablation row** alongside the conservative canonical. **De-confounding load moves to A6 (representation-level contrastive) and A7 (gradient-level adversary)** — see (M9) and (A4) in §14 for the full mechanism story.

### A6 — supervised contrastive pretraining

Phase 1 contrastive pretraining with speaker-masked positives (same Cold label, different pseudo-speaker; same-speaker same-class pairs masked out). Then attach the A5b head and continue. Batch composition: 8 pseudo-speakers × 8 chunks per batch.

### A7 — MDD/DANN speaker adversary

Gradient-reversal against the projection / head input, λ_adv ramped from 0. The biggest-upside, biggest-variance bet. Two-dimensional acceptance: UAR ≥ A6 − 1σ *and* probe top-1 drops ≥ 2σ vs A6.

### A5d — per-layer honesty diagnostic (DONE → A5e SKIPPED + structural finding)

**Hypothesis (from the SSL-speech literature).** Pasad et al. 2021 ("Layer-wise analysis of a self-supervised speech representation model") and Chen et al. 2022 / SUPERB report that for HuBERT-style SSL models — WavLM is in the same family — early Transformer layers concentrate **speaker identity / acoustic detail** while mid layers concentrate **paralinguistic / phonetic** content. A2's softmax layer-weights (locked) put most of their mass on L2–L6 — exactly the speaker-heavy band on those priors. That's *empirically* what motivated the test: a structural snapshot of where cold and speaker information live in the WavLM stack on URTIC.

**Recipe (no retraining).** For each WavLM layer L ∈ [0, 24]:

```text
cold_probe(pooled[:, L, :], y_cold)     →  cold_uar_L,    label_gain_L = cold_uar_L − 0.5
speaker_probe(pooled[:, L, :], y_pseudo) →  speaker_top1_L, speaker_gain_L = top1_L − 1/210
sub_at_1_L = label_gain_L − speaker_gain_L
```

Reads existing `cache/microsoft_wavlm-large/` chunks. Single seed (42), train_fit / devel_val splits. Cost ≈ 1 hour wall-clock. Output: `results/A5d_layer_honesty.csv`.

**Result (`results/A5d_layer_honesty.csv`).** Headline numbers across L ∈ [0, 24]:

```text
peak                              layer   cold_uar   speaker_top1   sub@1_L
best sub@1                        L21     0.5746     0.0406         +0.0387
best cold_uar                     L7      0.6052     0.0813         +0.0287
highest speaker_top1              L3      0.5769     0.0871         −0.0054
lowest speaker_top1               L22     0.5726     0.0402         +0.0371
```

**Structural finding.** Speaker top-1 decays roughly monotonically L0→L24 (0.0790 → 0.0429, range 0.087@L3 → 0.040@L22) — confirms Pasad 2021 / Chen 2022 layer-stratification *for speaker* on URTIC. Cold UAR by contrast is **flat** across the stack (range 0.56–0.61, σ ≈ 0.012) with no clean mid-band peak — refutes the mid-layer-cold hypothesis on URTIC. Reportable as a standalone paper finding independent of A5e's go/no-go.

**Verdict: A5e SKIPPED.** Both branches of the §5.6 trigger fired simultaneously — (1) no layer reaches `sub@1_L > 0.15` (peak +0.0387 ≪ 0.15), and (2) the cold-UAR peak (L7, 0.6052) coincides with the speaker peak (L7, top-1 0.0813) so a "rebuild on the cold-rich band" would be rebuilding on a speaker-rich band. Retrain track closed.

### A5e — WavLM mid-layer retrain (SKIPPED)

Was scoped to fire only if A5d's curve was dramatic — a clean mid-layer band with `sub@1_L > 0.15` and `speaker_top1_L` well below A2's full-stack 0.0501. Neither held (see A5d above). GPU goes to A5.5 / A6 instead.

### A5c — learned per-group gate (revivable)

Replaces fixed `mean_g` with a learned σ-gate per admitted group, residual L2-pulled toward zero. Was scoped to fire after A5b passed; A5b passes at K=1 — but a single-group fusion stack leaves little surface for a learned residual to refine over. On hold pending A5.5 / A6; revisit if a richer admission set re-opens.

Each subsequent rung must lower the speaker probe top-1 *without* dropping cold UAR materially. That's the two-dimensional acceptance discipline A2 set up and every rung inherits.

---

## 12. Key file map

```text
model/
  data/
    cached_dataset.py     PooledCacheDataset, stratified_split
    data.py               AudioDataset (raw waveform iterator)
  features/
    backbone.py           WavLM / HuBERT / Whisper interface
    extract.py            Batched pooled-stats extraction
    head.py               LayerWeightedPooledHead (A2's head architecture)
    head_a3.py            MannerAwareHead (rejected; kept as documented negative)
    manner.py             pYIN + RMS → 3-cat acoustic manner labels
    f0.py                 pYIN F0 contour, cached at cache/f0/
    opensmile_extract.py  eGeMAPSv02 functionals, cached at cache/handcrafted/egemaps/
    modulation.py         per-mel-band FFT-over-time → cache/handcrafted/modulation/
    scalar_g1.py          voicing scalars from manner labels (G1)
    scalar_g2.py          prosody scalars from F0 + manner labels (G2)
    scalar_g3.py          voice-quality carving of eGeMAPS (G3)
    scalar_g4.py          energy / pause / breath from waveform RMS (G4)
    scalar_g5.py          modulation-spectrogram aggregate, 64-d (G5)
    scalar_g6.py          spectral-shape carving of eGeMAPS (G6)
    ood_g8.py             Mahalanobis distance on A2-fused vectors (G8)
    train.py              train_head, sweep_threshold, predict_probs, evaluate
  honesty/
    probe.py              cold_probe + speaker_probe (matched linear LR)
    audit.py              audit_group; appends one CSV row per group
    fusion.py             A5b math (fit_cold_probe, predict_logit, zscore, fuse, sweep_tau)
  speakers/
    ecapa.py              ECAPA-TDNN embedding extraction
    cluster.py            KMeans pseudo-speaker assignment, load_pseudo_speakers
    probe.py              MLP speaker probe used in A2/A3 evaluation

cache/
  microsoft_wavlm-large/
    pooled/               per-stem WavLM pooled stats [25, 4096] fp16
    frames/L*/            per-stem WavLM frame hidden states (subset of layers)
    head_A2_seed*.pt      A2 head checkpoints (3 seeds)
    head_A3_seed*.pt      A3 head checkpoints (rejected)
  manner_labels/          per-stem 3-cat manner labels [T] int8
  f0/                     per-stem pYIN F0 contour [T] fp32 (NaN at unvoiced)
  handcrafted/
    egemaps/              per-stem eGeMAPSv02 functionals [88] fp32 + _columns.json
    g4/                   per-stem G4 energy scalars [11] fp32
    modulation/           per-stem G5 modulation features [64] fp32
  pseudo_speakers/
    k210_seed42.tsv       pseudo-speaker assignments

results/
  A2.json                 locked A2 metrics, random splits (3 seeds) + MLP probe
  A2_grouped.json         A2 retrained on grouped splits (3 seeds) + MLP probe + LR probe (audit)
  A2_grouped_honestprior.json  A2.5: A2 + honesty-prior layer-weight init at default lr (CANONICAL baseline; LOCKED)
  A2_grouped_honestprior_lr_stress.json  A2.5 layer-weight lr × {0.1, 1, 10, 100} stress test (single seed; mechanism control)
  A2_lr_init_grid.json    A2 lr × init grid: (uniform, honest_prior) × (lr×3,5,10) × 3 seeds (canonical-A2.5 confirmation; "different attractor")
  A3.json                 rejected A3 metrics (3 seeds)
  A5a_honesty.csv         per-group honesty rows (G1, G2, G3, G4, G4_gain_invariant, G5, G6, G8)
  A5b.json                A5b late-fusion sweep + locked devel_test eval (gate FAIL, run) + locked-K speaker probe + controls
  A5b_diag.json           A5b Pearson correlations + fused-vector speaker probe + redundancy-adjusted ranking
  A5b_ablation.json       K=2 ablation A2+G4+{G1,G5,G6} across 3 seeds + A2_τ calibration baseline (random splits)
  A5b_grouped.json        A5b K=1 lock on grouped splits, uniform-A2 anchor (PASS Δ +0.023)
  A5b_grouped_honestprior.json   A5b K=1 lock on A2.5 anchor, β=1.00 forced (FAIL — calibration failure under wrong-anchor β)
  A5b_grouped_honestprior_betasweep.json  A5b β-sweep on A2.5 anchor (PASS at β*=2.0, boundary-pegged; superseded by extended)
  A5b_grouped_honestprior_betasweep_extended.json  A5b extended β-sweep {0..16} + G4-alone baseline (LOCKED PASS at β=4-6 plateau, +0.035 over A2.5)
  A5_5_phase2_extract.json  A5.5 Phase 2: K=3 augmented pooled cache build (80.5% successful splices per variant, 10.9 min)
  A5_5_phase3_splice_detector.json   A5.5 Phase 3: splice-detector audit (FAIL at UAR 0.998 vs gate ≤ 0.55; gate-redefinition framing)
  A5_5_phase3p5_selfsplice_control.json   A5.5 Phase 3.5: self-splice control (UAR 0.9900, Δ -0.008 vs cross-splice → splicer broken → pivot to Plan B)
  A5_5_planB_phase2_mixup.json   A5.5 Plan B Phase 2-equiv: K=3 embedding-mixup cache (α∼U(0.70, 0.85), 100% successful, 1.13 min CPU)
  A5_5_planB_audit.json          A5.5 Plan B audit: detector UAR 0.5034 (chance) + mix-bit-as-cold 0.5000 (PASS gate B by class-balance)
  A5_5_planB_phase4_mixup.json   A5.5 Plan B Phase 4 conservative α∈[0.70,0.85] (LOCKED as A5.5 canonical): UAR Δ +0.006, probe ~unchanged → PARTIAL PASS (3-D gate FAIL on probe drop)
  A5_5_planB_phase2_mixup_aggro.json   A5.5 Plan B Phase 2-equiv aggressive α∈[0.50,0.70] (ABLATION): K=3 mixup cache, 100% successful, mean α 0.600, 1.13 min CPU
  A5_5_planB_phase4_mixup_aggro.json   A5.5 Plan B Phase 4 aggressive α∈[0.50,0.70] (ABLATION): UAR Δ -0.017 (σ 6×), probes still flat → branch (d) → narrow-window-EMPTY confirmed; closes α-axis
  A5d_layer_honesty.csv   per-layer cold + speaker probes on cached pooled[:, L, :] for L=0..24 (random splits)
  A5d_grouped_layer_honesty.csv   same per-layer probes on grouped splits
```

---

## 13. Decision diary (the "why" line by line)

In rough chronological order:

- **Frozen WavLM, not fine-tuned.** 9k training chunks + 316M parameters = guaranteed overfit. Frozen also gives a clean comparison axis for handcrafted features.
- **Layer-weighted pooled-stats over mean-pool.** Different WavLM layers encode different things; let the model learn which layers matter rather than throwing away that structure. Mean-pool is a special case (uniform weights, mean-only).
- **FeatureStandardiser.** Without it, raw pooled stds span 4 orders of magnitude and the head collapses to majority-class. Discovered the hard way during A2 debugging.
- **Balanced sampler over class-weighted loss.** Cleaner gradients, better-calibrated boundary; Huckvale showed this is preferable on URTIC.
- **Phoneme-CTC abandoned.** XLSR-CommonVoice domain-mismatched on URTIC's recording-booth German. Documented as negative result.
- **Manner labels via pYIN+RMS.** Replaced phonemes with a 3-cat acoustic regime label that's domain-robust and aligns frame-for-frame with the WavLM cache.
- **A3 head rejected.** Concat-then-MLP rediscovered speaker shortcut. Manner labels survived as conditioning info for A5.
- **Pseudo-speakers from ECAPA + KMeans, not WavLM-SV.** Architectural-circularity concern + WavLM-SV negative-control failure on URTIC.
- **Honesty audit — linear probes only.** If a feature group needs nonlinearity to predict cold, that's a signal it should be sub-divided, not blessed with hidden capacity.
- **eGeMAPSv02 over ComParE_2016.** 88 features vs 6,373 — fits comfortably under per-group dimensionality ceilings and the set is curated/physiologically motivated, not kitchen-sink.
- **G3/G6 carved by name prefix at runtime.** If we want to re-carve we don't re-run openSMILE.
- **F0 in a separate extractor, not folded into manner.py.** Manner cache is validated and committed across 19k chunks; mutating it risks breaking a working caching invariant.
- **G4 gain-invariant ablation.** Concern that absolute RMS is recording-gain confound. Ablation showed cols 4–10 alone retain almost all the lift with halved speaker leak. Admit the gain-invariant slice into A5b.
- **G8 dropped from admission pool.** Anti-predictive on devel_val. Documented negative — the original A5 hypothesis from the PDF doesn't hold here.
- **A5b: hard top-K, fixed β, fused-logit τ on train_threshold.** Cleanest paper story; sharpest ablation against A5c if we run it.
- **G5 (modulation spectrogram) added late.** Cross-comparison with the 2017 ComParE submissions surfaced Huckvale's MOD family as the one feature class we hadn't covered with G1-G4/G6. Modulation rate captures cross-frame envelope dynamics (syllable rate, breath pacing) that none of the per-frame static features see. Aggressively aggregated to 64-d so the linear-only probe can't launder speaker identity through capacity (same discipline as G3/G6).
- **A5b diagnostics added (correlations + fused-vector speaker probe).** `sub@1` measures honesty in isolation; it doesn't see redundancy with A2 or with other admitted groups, so K ≥ 2 over highly-correlated groups looks beneficial in the table but does little in fusion. A 4×4 / 5×5 Pearson matrix + per-group argmax-disagreement-vs-A2 makes that visible. Separately, plan.md § 5.7's "speaker probe top-1 ≤ A2 + 1σ" gate is degenerate on a 1-d fused logit — the fused-vector concat probe is the honest operationalisation.
- **A5b sweep window extended to K=4.** A5a placed G5_modulation at #4 in the admission ordering, only 0.005 sub@1 below G6. With the original K∈{1,2,3} window, G5 could not compete despite being honest. Extending one slot lets the val sweep include it; K=4 won on 2/3 seeds, which is also where most of the A5b variance lives (seed 7 picked K=2). Cleaner to extend and report than to claim a finding from an arbitrarily truncated grid.
- **Redundancy-adjusted ranking is a paper diagnostic, not an admission-rule replacement.** The temptation after A5b's gate FAIL was to swap `sub@1` for `safe_unique_gain = label_gain·(1−r²) − speaker_gain` and re-admit groups under the new key. Decided against — moves the goalposts after the test ran. Ships as a paper diagnostic column instead, so reviewers can see "what does each group uniquely contribute over A2?" without us having retroactively reshaped the criterion to fit the numbers.
- **A2_τ calibration baseline added alongside A5b.** Vanilla A2 reports argmax UAR; A5b reports tuned-τ UAR. Without an A2_τ control, the A5b lift conflates "fusion adds signal" with "tuning τ moves the threshold." A2_τ is `evaluate_at_tau(A2_logits, τ_swept_on_train_threshold)` — same calibration discipline, no fusion. Reported as Δ vs A2_argmax *and* Δ vs A2_τ in the K=2 ablation table.
- **A5d (per-layer probe) before A5e (mid-layer retrain).** When A5b failed the gate, the immediate temptation was to push deeper into the fusion stack — more groups, learned βs, etc. Held: ran a cheap (~1 hour, no retraining) per-layer cold + speaker probe on the *existing* WavLM cache instead. Pasad/Chen's layer-stratification result is a *generic* claim about SSL speech models; whether it holds *on this corpus* is what decides if A5e is worth the GPU. Diagnose before treating.
- **A3-style retrain refused as a band-mask shortcut.** A natural reading of A5d would be "just train A2 on the mid layers and call it done." Refused: A3 already showed that re-pooling WavLM by an extra structural axis (manner categories) reintroduced speaker leak even when the labels themselves were clean. A5e is therefore *not* a new pooling axis — it's the same A2 head with the layer-weight softmax restricted to a band, plus rerun fusion on top.
- **K-locked ablation flipped A5b from FAIL to PASS.** The original free K∈{1..4} sweep produced UAR 0.6502 ± 0.0078 (σ > effect size, FAIL). Locking admission to {K=1, K=2 each pair} and sweeping only β/τ gave A2+G4_gi (K=1) at UAR 0.6576 ± 0.0011 — the σ collapse 0.0112 → 0.0011 (7×) is the diagnostic. Free K-sweep on `train_threshold` over-rewards configs with more τ flexibility (more groups widen the achievable τ-vs-UAR curve), inflating variance without improving the mean; locking K removes the over-fit channel. Both numbers ship in the paper: K=1 as the headline (Δ +0.0148 ± 0.0045 vs A2_argmax, +0.0112 ± 0.0066 vs A2_τ; gate cleared by ~2σ), K=4 as the documented sweep-protocol finding.
- **A5d demoted to paper-only after A5b PASS.** A5d was scoped as the rescue route for an A5b FAIL — a cheap (~1 hour, no retraining) per-layer probe to motivate a band-restricted backbone retrain (A5e). With A5b passing at K=1 the rescue is no longer load-bearing. A5d still ships because the layer-stratification claim is reportable on its own (Pasad/Chen on URTIC); A5e fires only if A5d shows a *dramatic* band, otherwise the GPU goes to A5.5 / A6.
- **K=1 PASS reorders the post-A5 ladder.** Originally A5d → A5e → A5.5/A6/A7, on the assumption A5b would need a structural rescue. With A5b passing via a single audited group on top of the existing A2 backbone, A5.5 (cross-speaker splicing) becomes the next gate, A5d ships as a paper diagnostic, A5e likely doesn't fire. The de-confounding interventions (A5.5 → A6 → A7) move up; the rescue track moves down.
- **A5d ran: A5e SKIPPED + structural paper finding.** The per-layer probe over L ∈ [0, 24] closed the retrain track: both branches of the A5e trigger fired simultaneously — peak `sub@1_L` is L21 = +0.0387 (≪ 0.15 trigger) and the cold-UAR peak (L7, 0.6052) coincides with the speaker peak (L7, top-1 0.0813), so any "rebuild on the cold-rich band" would also be a rebuild on a speaker-rich band. Standalone finding worth reporting: speaker top-1 decays roughly monotonically L0→L24 (0.087@L3 → 0.040@L22) — **confirms Pasad 2021 / Chen 2022 layer-stratification for speaker on URTIC** — but cold UAR is **flat** across the stack (range 0.56–0.61) — **refutes mid-band-cold hypothesis on URTIC**. Retrain track closed; GPU goes to A5.5 / A6.
- **Locked-K speaker probe + capacity controls flipped (ii) FAIL → PASS via probe-substrate correction.** First read of the locked-K cell looked like probe (ii) backbone-concat FAIL: top-1 0.0675 > 0.0510 ceiling. Capacity controls run after to disambiguate "real G4↔pooled interaction info" from "probe got 7 more dims to play with" came back saying the (a) pooled-alone probe under the *same code path* (`honesty.speaker_probe`, multinomial LR) lands at 0.0674 ± 0.0006 — i.e. the 0.0510 ceiling was anchored on a different probe substrate (`speakers/probe.py`'s deeper MLP, the historical 0.0501 number from `A2.json`). Apples-to-apples ceiling under the LR substrate is 0.0680; both gates PASS (probe (i) 0.0119 by ~6×, probe (ii) 0.0675 by 0.0005). G4_gi adds +0.0001 to speaker recoverability above pooled-alone — essentially zero. The (i)–(ii) gap (0.0119 vs 0.0675, ~5.5 pp drop) is the per-channel cold probe acting as a speaker-information bottleneck *by design* — clean architectural validation of choosing logit-level fusion over A3's concat-MLP. Lesson: when comparing probe top-1 values, always confirm the substrate matches; same features under MLP vs LR can read 0.0501 vs 0.0674.
- **Within-partition leak audit closed (plan §4.5 DONE).** `stratified_split` was per-class random shuffle, so train_fit/train_threshold and devel_val/devel_test shared 198/210 + 198/206 pseudo-speakers under `split_seed=42`. New `stratified_grouped_split` (sklearn `StratifiedGroupKFold` keyed on the k=210 pseudo-speakers) gives 0/0 overlap. A2 retrained on grouped splits (3 seeds, `results/A2_grouped.json`): argmax UAR drops -0.0067 (~2σ — argmax was mildly inflated by leak); calibrated UAR essentially unchanged (+0.0034); val-test gap moves from -0.0009 (random) to -0.0133 (grouped) — correctly revealing speaker-disjointness instead of hiding it. MLP speaker probe top-1 0.0501 → 0.0498 (unchanged — confirms the cross-partition disjointness was the load-bearing one for speaker-probe interpretation). LR speaker probe top-1 0.0760 ± 0.0020 (codepath-consistent ceiling for A5b/A5d audits, partly higher because grouped devel_val has fewer distinct true-speaker classes ~103 vs ~206 random). The honest framing for the paper: "we audited a suspected within-partition leak, found and quantified it, and report both the historical and leak-corrected baselines" — the audit itself is the contribution, not a discovered crisis.
- **A5b/A5d re-run on grouped splits — K=1 PASS strengthened, A5d structural finding survives.** A5b K=1 (`results/A5b_grouped.json`, 3 seeds, `head_A2grouped_seed{seed}.pt` anchor): UAR 0.6588 ± 0.0059, **Δ vs A2_argmax = +0.0227 ± 0.0059 (3.8σ — was +0.0148 ± 0.0045 / 3.3σ on random splits)**. The fusion lift got *larger* under grouped splits because A2's argmax baseline was the inflated part of the random-split number (-0.0067) while K=1 fused UAR is essentially unchanged (+0.0012). Cleanest possible methodology-fix outcome. Locked-K speaker probes (3 seeds, grouped LR ceiling 0.0780): probe (i) literal 2-D 0.0153 ± 0.0032; probe (ii) backbone-concat 0.0733 ± 0.0002 — both PASS. A5d per-layer (`results/A5d_grouped_layer_honesty.csv`): structural shape preserved — speaker still monotone L0→L24 (0.072→0.042), cold UAR still flat (0.56–0.61). Best `sub@1` shifted L21→L0 (early cold-rich layers re-rank above late ones because they're speaker-rich early too) but absolute sub@1 stays small (+0.040), so A5e SKIPPED holds — both trigger conditions still fire on grouped. A5b_diag and A5b_ablation (K=2 ablations) ship as paper diagnostics about the K-sweep pathology, which is split-independent — not re-run.
- **A2.5 honesty-prior init: A5d converted from descriptive to architectural prior.** Side-experiment motivated by the reflection's question — "is A5d's per-layer signal architecturally exploitable?" Initialised A2's softmax layer-weights with `T_INV * sub@1` (T_INV=50) instead of zeros. Result: **+0.020 UAR (4.7σ) free**, MLP probe flat (0.0501 → 0.0501), LR probe slightly down (0.0760 → 0.0725). Pareto improvement on every measured dimension. Initial mechanistic finding: `cos(init_softmax, final_softmax) = 0.9998` across all seeds — the optimizer literally did not move the prior. **This was initially read as "flat loss landscape" but the lr stress test (next entry) revised the mechanism.** A2.5 becomes the new canonical baseline for downstream rungs (A5.5, A6, A7); A5b K=1 will be re-evaluated on this anchor before its verdict is locked.
- **A2.5 lr stress test contradicts the flat-landscape claim — mechanism revised.** Re-trained A2.5 with layer-weights lr boosted across `lr_factor ∈ {0.1, 1.0, 10.0, 100.0}` × base_lr (default 0.1×). Result (single seed, `results/A2_grouped_honestprior_lr_stress.json`): cos(init, final) drops 0.9999 → 0.9953 → 0.8302 → 0.2083 across the lr range. There IS gradient signal in the layer-weight subspace — the standard A2 recipe just doesn't surface it (default lr 1e-4 + 25-epoch / patience-6 horizon). At lr×10 the optimizer finds different layer mixes (lost L22/L24, picked up L6/L7) reaching UAR 0.6599; at lr×100 it overshoots into a degenerate L4-dominated solution (max/min ratio 20120×, UAR 0.6443). Goldilocks zone around lr×10. **Revised paper claim:** *"data-derived layer-weight init produces +0.020 UAR over uniform init in the standard low-lr regime, where the optimizer is constrained from moving regardless of starting point. Practically useful within standard optimization settings rather than fundamentally necessary across all settings."* Narrower but bulletproof. Side-finding worth noting: lr×100's degenerate attractor concentrates on L4 (cold UAR 0.581, sub@1 ~0.012 per A5d) — independent cross-validation that A5d's audit signal points away from optimization-bad attractors.
- **A5b on A2.5 — β=1.00 forced FAIL was calibration failure, not signal absence.** Protocol-strict re-run with β=1.00 on the new A2.5 anchor (`results/A5b_grouped_honestprior.json`) gave Δ -0.022 vs A2.5_argmax with τ pegged at search edge -3.95 across all 3 seeds. Smoking gun: β=1.00 was implicitly conditioned on the uniform-A2 anchor; under A2.5's more-confident logits it over-weights G4_gi, creating a fused logit distribution where no τ in the search range generalises from train_threshold to devel_test. The calibration-failure diagnosis from the second reflection led to the β-sweep audit (next entry).
- **A5b β-sweep on A2.5: PASS recovered at β*=2.0 (boundary-pegged, caveated).** β grid {0, 0.05, 0.1, 0.25, 0.5, 0.75, 1.0, 1.5, 2.0} swept jointly with τ on train_threshold (`results/A5b_grouped_honestprior_betasweep.json`). All 3 seeds locked β*=2.0 (the upper edge of the grid); K=1 fused UAR 0.6755 ± 0.0208, Δ +0.019 ± 0.018 vs A2.5_argmax. **Caveat at the time: β* at search boundary** (same diagnostic that flagged K-sweep + β=1.00 fail). Verdict provisional pending extended β grid + G4-alone baseline + A2.5 lr × init grid.
- **A2.5 lr × init grid confirms canonical: prior is a different attractor.** 18 retrains, (uniform, honest_prior) × (lr×3, 5, 10) × 3 seeds (`results/A2_lr_init_grid.json`). **Findings:** uniform plateaus at UAR 0.6447–0.6466 across all tested lr (never reaches 0.656+); honest_prior reaches 0.657–0.664 at every lr. Δ(prior − uniform) at lr×10 = +0.019 with prior having tighter σ (0.0034 vs 0.0070). **The honesty prior is a *distinct attractor* in the layer-weight subspace that uniform init does not reach via lr alone** — strongest formulation of the (A1) paper claim earned so far. Speaker probe inflates with higher lr in both regimes (uniform: 0.076 → 0.082; prior: 0.0725 → 0.088); the lr-induced concentration on early speaker-rich layers (top-5 shifts toward L0/L4/L5) is the mechanism. **A2.5-default (honest_prior + lr×0.1) stays canonical** — Pareto-dominates honest_prior + lr×3 (+0.0008 UAR for +0.008 spk leak; same UAR, more leak). honest_prior + lr×10 trades +0.007 UAR for +0.016 spk leak — bad trade for de-confounding paper narrative. The lr × init grid serves as ablation evidence ("prior contribution is robust across lr regimes"), not a replacement.
- **A5b extended β-sweep + G4-alone baseline: LOCKED PASS at +0.035 over A2.5.** β grid extended with {2.5, 3, 4, 6, 8, 12, 16} (`results/A5b_grouped_honestprior_betasweep_extended.json`). Per-β UAR climbs sharply β=2 → β=4, plateaus at 0.6875–0.6917 across β ∈ [4, 16], drops to 0.6632 at β=∞ (G4-alone). **Interior optimum confirmed** — not a boundary artifact. Per-seed argmax-locked β* = [6, 8, 4] (none at boundary); K=1 fused UAR **0.6913 ± 0.0076**, Δ +0.0349 ± 0.0054 vs A2.5_argmax (~6.5σ, strong PASS). G4-alone (β=∞) hits UAR 0.6632 — **striking parity with A2.5-alone 0.6564** (Δ ~0.7σ; a 7-dim handcrafted feature matches a 4096-dim WavLM-Large representation). K=1 fused beats G4-alone by +0.028 — fusion is genuinely additive, A2.5 contributes real cold signal that G4_gi alone doesn't carry. Speaker probes are β-independent (β only enters the cold classifier's decision rule, not the probe's input space): probe (i) = 0.0156 (≪ 0.078), probe (ii) = 0.0733 (≤ 0.078) — both PASS by wide margins. **Three reportable lock options:** β=4 robust (UAR 0.6875 ± 0.0111, Δ +0.031, conservative); β=6 peak (UAR 0.6917 ± 0.0085, Δ +0.035); per-seed argmax (UAR 0.6913 ± 0.0076, Δ +0.035, protocol-strict headline). All three within ~1σ in the plateau. **Total stack uniform-A2-grouped → A2.5 → A5b K=1 = +0.055 UAR** (~10σ on the leak-corrected baseline σ).
- **A5.5 Phase 2 extraction CLEAN, Phase 3 audit FAIL at UAR 0.998 — gate redefinition vs splicer-broken interpretation pending Phase 3.5 self-splice control.** Phase 2 (K=3 augmented pooled cache for grouped train_fit, `results/A5_5_phase2_extract.json`) ran clean: 80.5% successful splice rate replicates from smoke (77.3% → 80.5% at corpus scale), three variants are independent (no top-5 partner-reuse overlap across k), 10.9 min on GPU. Phase 3 splice-detector audit (`results/A5_5_phase3_splice_detector.json`): binary LR probe distinguishes original vs spliced pooled features at **UAR 0.998 across every layer (L0/4/7/12/18/24) and every k variant** — perfect detector vs gate target ≤ 0.55. **Diagnosis is genuinely ambiguous between two interpretations** at this saturation level: (a) "gate is miscalibrated for cross-speaker splicing" — pooled stats are mean+std+skew+kurt over time per WavLM layer; replacing 25-30% of a chunk with audio from a different speaker shifts per-layer time-means by ~25-30% toward partner's speaker characteristics (the same property ECAPA exploits); cross-speaker splicing is *supposed* to make chunks acoustically different; the 0.55 gate was likely calibrated for perceptual-equivalence augmentations (time-warp/noise) and is unachievable for cross-speaker splicing by construction. (b) "splicer technique itself leaves loud artifacts beyond cross-speaker mixing" — detection is layer-uniform (L0=0.992 ≈ L24=0.995), but speaker info concentrates in early layers per A5d; if cross-speaker mixing were the only loud signal the detector should be stronger early; layer-uniformity suggests crossfade/spectral-boundary/recording-session artifacts on top. **Caveat: at UAR 0.998 the layer-distribution argument is only suggestive (saturation).** Importantly, the *actual* shortcut concern (splice-presence correlated with cold) is structurally mitigated regardless: symmetric-across-classes partner-pool means every chunk regardless of cold gets K=3 augmented variants, splice presence is class-balanced, model can detect "this is spliced" with UAR 0.998 *and still not be able to use it to predict cold*. Phase 3.5 self-splice control (queued, ~3 min) — splice anchors with same-pseudo-speaker partners (no cross-speaker mixing) and re-run the same detector — distinguishes the two interpretations: self-splice UAR < 0.6 → splicer clean, gate redefinition justified, proceed to Phase 4; 0.6-0.85 → splicer fixes attempted; > 0.85 → pivot to embedding mixup (Plan B). See [plan §4.8](plan.md) for full phase documentation + Plan B framing.
- **A5.5 Phase 3.5 self-splice control verdict: branch C (splicer broken) — audio splicing DEAD, pivoting to Plan B (embedding mixup).** Self-splice UAR 0.9900 ≈ cross-splice UAR 0.9981 (Δ layer-averaged = -0.008; per-layer Δs all in [-0.030, -0.005]). **Cross-speaker mixing accounts for <1% of the detectability; the splicing OPERATION itself accounts for ~99%.** This rules out an entire class of fixes — no partner-selection refinement (same-session, same-speaker, RMS-matched) will help; the splice operation produces a signature regardless of who you splice with. Splicer-internal fixes (longer crossfade 250→400ms, tighter low-energy boundaries, smaller r, spectral-envelope matching at the seam) might shave 5-10 UAR points but won't drop 0.99 → 0.55 (a 40+ point gap). The fundamental issue: WavLM's CNN feature-extractor + transformer attention are sensitive to splice-induced spectral discontinuities at every layer (L0 = 0.98, L24 = 0.965 — uniform). **Methodological warning (paper-worthy, transferable):** "Audio-level cross-speaker splicing on URTIC WavLM representations is detectable at >99% UAR even when the operation is performed within a single speaker. The splicing operation itself, not the cross-speaker mixing, is the source of the artifactual signal. Anyone doing audio-level cross-speaker augmentation on foundation-model representations should run a self-splice control before proceeding to training." Generalises beyond URTIC. **Pivot to Plan B (embedding mixup, plan §4.8.5):** mix WavLM pooled stats per chunk (`α · anchor + (1-α) · partner`, α ∼ U(0.70, 0.85), partner = same Cold + different pseudo-speaker) instead of audio. Sidesteps every audio-level artifact (no crossfade, no boundary, no session mismatch — pure tensor blend). Pure CPU op on cached pooled stats; ~1-2 min for K=3 on 8532 chunks. Revised gate framing for ALL cross-speaker augmentations: detector UAR is *descriptive* (augmentation-class baseline; expected non-zero by construction); the actual shortcut gate is **mix-bit-as-cold-predictor ≤ 0.52** (~0.5 expected by class-balanced design). The original 0.55 detector hard threshold was always wrong as a shortcut gate; it served its diagnostic purpose (caught the splicer-artifact problem via Phase 3.5) but not as the actual safeguard. Plan B Phase 2-equiv build + audit + Phase 4 training (warm-start from A2.5, per-epoch sampling from {original, mix_0/1/2}, devel unaugmented) queued.
- **A5.5 Plan B Phase 4 aggressive-α sweep ran → branch (d) → A5.5 LOCKED at conservative-α as canonical; narrow-window-empty confirmed.** (`results/A5_5_planB_phase2_mixup_aggro.json` + `results/A5_5_planB_phase4_mixup_aggro.json`, α ∼ U(0.50, 0.70), 3 seeds, 1.13 min CPU cache + 4.10 min Phase 4.) Cache integrity: 100% successful mixes across all k, mean α = 0.600, partner-class balance ~9% cold (matches corpus), no partner-pool degeneracy. Phase 4: UAR 0.6397 ± 0.0186 (Δ -0.017 vs A2.5, σ exploded ~6×); recC 0.404 ± 0.123 (variance exploded); MLP probe 0.0485 (Δ -0.0017, within noise); LR probe 0.0735 (Δ +0.0010, within noise); **cos(init_A2.5, final) = 0.99998 across all 3 seeds — EVEN MORE locked at A2.5 init than the conservative variant**; **best_epoch = {1, 1, 1}** — converges immediately on epoch 1, then training degrades (label-validity damage from partner-dominant mix). 3-D gate: gate 1 (UAR floor 0.6525) FAIL by -0.013; gates 2a (MLP ≤ 0.0456) FAIL by +0.003; gate 2b (LR ≤ 0.0723) FAIL by +0.001; gate 3 PASS by construction (α-independent). **Verdict: branch (d, unexpected) — the cleanest possible disposition for locking.** Branches (a/b/c) would have left "try another α" open; branch (d) closes the α-axis by showing both endpoints fail differently (conservative too gentle to de-confound; aggressive damages label validity without recovering de-confounding) — intermediate ranges (e.g. U(0.60, 0.80)) can't escape because gentler falls toward conservative's null and stronger toward aggressive's UAR damage. **A5.5 LOCKED: conservative α ∈ [0.70, 0.85] as canonical** (UAR Δ +0.006, probe within noise, recall pattern more cold-balanced); **aggressive α ships as the ablation row that closes the α-axis** (extends M9 → narrow-window-empty finding). Per the reflection's deeper diagnosis: mixup on post-frozen-backbone representations cannot de-confound without an explicit objective; A6 (contrastive, speaker-masked positives) and A7 (MDD/DANN adversary) are the architecturally necessary rungs for the speaker-invariance claim. Paper framing as 3-level de-confounding story: A5.5 = data-level (locked, modest contribution, evidence that data alone is insufficient); A6 = representation-level; A7 = gradient-level. A5.5's modest result is itself the load-bearing evidence for *why* A6/A7 are necessary.
- **A5.5 Plan B Phase 2 + audit BOTH PASS clean; Phase 4 PARTIAL PASS (UAR floor PASS, speaker probe drop FAIL on strict 3-D gate); α ∈ [0.5, 0.7] sweep queued.** Plan B Phase 2-equiv embedding-mixup cache build (`results/A5_5_planB_phase2_mixup.json`) ran clean: 100% successful mixes (no fallback), α distribution mean 0.775 ± 0.043 in U(0.70, 0.85) range, partner-class balance 10.1% cold ≈ corpus 9.5% (symmetric), 1.13 min CPU. Plan B audit (`results/A5_5_planB_audit.json`) PASSED both gates: (A) detector UAR layer-averaged 0.5034 — essentially CHANCE, **Δ -0.4947 vs Phase 3 audio splicing 0.998** (the smooth tensor-blend completely sidesteps the artifactual signal); per-layer L0/4/7/12/18/24 all in [0.501, 0.505]; per-k all in [0.500, 0.502]. (B) Mix-bit-as-cold-predictor 0.5000 (trivial PASS by class-balanced augmentation construction; cold rate among original = 0.1017, among mixed = 0.1017, Δ exactly +0.0000). **Plan B Phase 4 training** (`results/A5_5_planB_phase4_mixup.json`, 3 seeds {42, 123, 7}, warm-start from A2.5 checkpoints): UAR 0.6624 ± 0.0031 (Δ +0.006 vs A2.5 0.6564, ~1.6σ above zero); per-class recall shifted to more cold-balanced (recC 0.43 → 0.47, recNC 0.87 → 0.85); MLP probe top-1 0.0506 ± 0.0019 (Δ +0.0005 vs A2.5 0.0501 — essentially unchanged); LR probe top-1 0.0759 ± 0.0030 (Δ +0.0034 vs A2.5 0.0725 — slightly UP, within noise); val_test_gap -0.0291 (more pessimistic than A2.5's -0.020). **Critical diagnostic: cos(init_A2.5, final) = 0.9999, 1.0000, 0.9997 across all 3 seeds — the optimizer DID NOT MOVE the layer weights from A2.5's converged state.** Best epochs are early (3, 1, 5) with training plateauing or worsening after epoch 5. The +0.006 UAR comes entirely from MLP/classifier-level adjustments to the augmented training distribution, NOT from layer-weight refinement (consistent with M5: layer-weight subspace has weak gradient signal at default lr). **3-D acceptance gate verdict: gate 1 (UAR floor 0.6525) PASS by +0.010; gate 2a (MLP probe drop ≤ 0.0456) FAIL by +0.005 (probe slightly UP); gate 2b (LR probe drop ≤ 0.0723) FAIL by +0.004 (probe slightly UP); gate 3 (mix-bit shortcut) PASS by construction. Overall: FAIL on strict 3-D gate.** Diagnosis: α=0.775 mean = 22.5% partner contribution is too gentle to push the model toward speaker-invariance. The audit detector at 0.503 confirms mixed pooled vectors are statistically indistinguishable from natural ones — the model has nothing to "learn invariance to." Augmentation activated weak regularization (recall pattern shifted) but not de-confounding. Recall-balance shift +0.04 recC / -0.02 recNC is meaningful for minority-class detection (worth reporting beyond UAR scalar). **α ∈ [0.5, 0.7] sweep queued (plan §4.8.8)** to test whether 35-50% partner contribution activates the de-confounding mechanism. Three branches: (a) UAR holds + probe drops → strict PASS, lock aggressive variant as canonical A5.5; (b) UAR holds + probe stays → confirmed limitation: per-chunk pooled-stat mixing too gentle for speaker-shortcut attack on URTIC's WavLM, lock as "modest UAR / null on probe", move to A6; (c) UAR drops + probe drops → Pareto trade-off documented. Cost ~30 min compute.
- **Probe substrate × split protocol matrix is now a methodology requirement, not a footnote.** Three independent dimensions affect speaker-probe top-1 readings on the same A2 fused vector: probe architecture (MLP vs LR — gives 0.05 vs 0.07 on identical features), split protocol (random vs grouped — gives 0.067 vs 0.076 because random shares 198/210 pseudo-speakers and the probe partly recognises rather than generalises), and audit context (which "ceiling" is the right comparison reference). Paper methodology section needs a substrate × protocol matrix; gates only compare apples-to-apples within a row of the matrix. The cleanest gate language for A5b/A5d going forward: "probe top-1 on the A5b representation under `honesty.speaker_probe` on grouped splits ≤ A2 LR-grouped + 1σ = 0.0780".

If you've read this far, the rest is just running cells and watching numbers. The methodological scaffold is the contribution — the UAR is the cherry.

---

## 14. Paper-ready findings (running list)

A catalog of the bankable contributions earned so far, organised by the paper section each fits into. Each entry includes the result, why it matters, and where in the repo to find the evidence. Update this list as new findings land.

### 14.1 Methodology contributions

**(M1) Within-partition speaker-leak audit + fix** ([plan §4.5](plan.md), [results/A2_grouped.json](results/A2_grouped.json)). `stratified_split` shared 198/210 pseudo-speakers between train_fit/train_threshold and 198/206 between devel_val/devel_test. New `stratified_grouped_split` (sklearn `StratifiedGroupKFold` keyed on the k=210 pseudo-speakers) gives 0/0 overlap. Argmax UAR was inflated by ~1pp under random splits (0.6428 → 0.6361 grouped); val-test gap correctly reveals devel_val/devel_test speaker disjointness (0.000 → -0.013). MLP speaker probe stable (0.0501 → 0.0498) — confirms the cross-partition disjointness was the load-bearing one for the speaker-probe interpretation. "We audited a suspected within-partition leak, found and quantified it, report both baselines."

**(M2) Probe substrate × split protocol matters for honesty gates** ([EXPLAINER §6](EXPLAINER.md), [plan §5.7](plan.md)). The same A2 fused vector gives top-1 = 0.0501 (MLP probe, ~30 epochs) vs 0.0674–0.0760 (LR probe, LBFGS) — different substrates, different numbers. Random vs grouped also gives different numbers because random-split probes partly *recognise* shared speakers, while grouped-split probes measure *generalisation across unseen pseudo-speakers* (the strictly more honest measurement). Going forward, gates anchor on `honesty.speaker_probe` substrate AND grouped splits — ceiling = A2 LR-grouped + 1σ = 0.0780. Methodology section needs a substrate × protocol matrix.

**(M3) Per-layer honesty audit (A5d)** ([plan §5.5](plan.md), [results/A5d_layer_honesty.png](results/A5d_layer_honesty.png), [results/A5d_grouped_layer_honesty.csv](results/A5d_grouped_layer_honesty.csv)). For each WavLM layer L ∈ [0,24], audit cold-probe and speaker-probe top-1 on cached `pooled[:, L, :]`. Findings on URTIC: speaker top-1 decays ~monotonically L0→L24 (0.087 → 0.043 random, 0.072 → 0.042 grouped — confirms Pasad 2021 / Chen 2022 layer-stratification for the speaker axis); cold UAR is **flat** across the stack (range 0.56–0.61) — refutes the strong form of "mid-band paralinguistic = cold-relevant" on URTIC specifically. Reportable as standalone empirical finding regardless of whether it's used to drive architecture.

**(M4) τ-sweep pathology in free-K honesty fusion** ([plan §5.7](plan.md), [results/A5b.json](results/A5b.json) vs [A5b_ablation.json](results/A5b_ablation.json)). Free K∈{1..4} sweep on `train_threshold` produced UAR 0.6502 ± 0.0078 (FAIL: σ > effect size). K-locked K=1 ablation collapsed σ 0.0112 → 0.0011 (7×) and lifted mean — "more groups widen the achievable τ-vs-UAR curve, the val sweep over-rewards that flexibility." Diagnostic for any honesty-fusion protocol that sweeps K together with τ. **Generalises to β-sweep too** (M4-bis, see (A2) below): the same boundary-pegging behaviour appears when β has more freedom than the train_threshold can constrain. The protocol-level lesson is "any sweep dimension on a small held-out fold is at risk of edge-pegging when the underlying signal is weak."

**(M5) Layer-weight subspace has gradient signal at higher lr (A2.5 lr stress test)** ([plan §4.6.1](plan.md), [results/A2_grouped_honestprior_lr_stress.json](results/A2_grouped_honestprior_lr_stress.json)). Re-trained A2.5 with `layer_weights lr ∈ {1e-4, 1e-3, 1e-2, 1e-1}` (factors 0.1×, 1×, 10×, 100× of base_lr 1e-3). cos(init, final) drops 0.9999 → 0.9953 → 0.8302 → 0.2083 across the lr range — the layer-weight subspace is NOT flat in any absolute sense; the standard A2 recipe (s3prl-convention 0.1× factor + 25-epoch / patience-6 horizon) is just too constrained to surface the gradient signal. UAR is non-monotonic in lr (peak at lr×10 = 0.6599; lr×100 = 0.6443 = degenerate one-hot on L4). **The Goldilocks zone is around lr×10.** Side-finding: lr×100's degenerate attractor is L4 (cold UAR 0.581, sub@1 ~0.012 per A5d) — independent cross-validation that the audit's signal points away from optimization-bad attractors. This finding *revises* the original (A1) "flat landscape" claim — the prior contribution is "practically useful at standard low-lr" rather than "fundamentally necessary across all lr regimes." The single-seed lr×10 result also hints that prior + medium-lr might dominate prior + standard-lr; pending 3-seed lr × init grid (§4.6.2) to confirm.

**(M6) Audit cross-validation via optimization-failure mode** ([(M5) above](EXPLAINER.md)). The lr stress test's degenerate attractor at lr×100 concentrates >85% weight on L4 — a layer A5d explicitly flags as low sub@1 (0.012, near the bottom of the per-layer honesty curve). This is independent evidence that the A5d audit's per-layer scoring is meaningful: the optimizer's "bad" attractor (the one that hurts UAR) lands on a layer the audit identifies as honesty-poor. A clean piece of cross-validation for the audit's signal that doesn't require any new measurement.

**(M7) Striking parity: 7-dim handcrafted feature ≈ 4096-dim foundation-model representation on chunk-level UAR** ([plan §4.7.3](plan.md), [results/A5b_grouped_honestprior_betasweep_extended.json](results/A5b_grouped_honestprior_betasweep_extended.json)). G4-alone (β=∞ limit of the A5b fusion: G4_gain_invariant's 7-dim cold-probe logit with τ swept on train_threshold) reaches devel_test UAR **0.6632 ± 0.0000** — within ~0.7σ of A2.5-alone's 0.6564 ± 0.0038. A 7-dim deterministic handcrafted feature (gain-invariant energy/pause/breath stats from waveform RMS) matches a 4096-dim WavLM-Large pooled-stats representation on chunk-level cold UAR. Two interpretations worth noting in the paper: (a) G4_gi's gain-invariant slice happens to be unusually well-suited to URTIC cold detection — the "remove absolute amplitude, keep regime-contrasts" design choice was load-bearing; (b) the chunk-level cold signal has a low ceiling around UAR 0.66 that any reasonable representation hits, with the differentiating factor being whether you can cleanly fuse independent views. The K=1 fused result (0.6913, +0.028 over G4-alone, +0.035 over A2.5-alone) settles that A2.5 and G4 capture *partially orthogonal* cold information — both individually approach the same per-feature ceiling, but their combination breaks past it via fusion. Worth a sentence in the methodology section as evidence that G4 isn't redundant with the foundation-model representation.

**(M8) Audio-level cross-speaker splicing on foundation-model representations is detectable at >99% UAR even within a single speaker — the splicing operation itself is the artifact source. Pivot to embedding mixup as the data-level de-confounding intervention.** ([plan §4.8.3 / §4.8.4 / §4.8.5](plan.md), [results/A5_5_phase3_splice_detector.json](results/A5_5_phase3_splice_detector.json), [results/A5_5_phase3p5_selfsplice_control.json](results/A5_5_phase3p5_selfsplice_control.json)). Phase 3 of A5.5 found a binary LR probe distinguishes original vs *cross-speaker* spliced WavLM pooled features at **UAR 0.998** across every layer (L0/4/7/12/18/24) and every k variant. Phase 3.5 self-splice control — splicing anchors with partners from the same pseudo-speaker cluster (no cross-speaker mixing) — gave **UAR 0.9900**, only Δ -0.008 below the cross-splice number. **Cross-speaker mixing accounts for <1% of the detectability; the splicing operation itself accounts for ~99%.** Layer-uniform detection (L0=0.98 ≈ L24=0.965 self-splice) confirms WavLM's CNN feature-extractor + transformer attention pick up splice-induced spectral discontinuities at every layer, regardless of partner identity. No partner-selection refinement (same-session, same-speaker, RMS-matched) can fix this; splicer-internal fixes (longer crossfade, tighter boundaries, smaller r, spectral-envelope matching) might shave 5-10 UAR points but won't drop 0.99 → 0.55 (a 40+ point gap). **Pivoted to embedding mixup** (plan §4.8.5): mix WavLM pooled stats per chunk (`α · anchor + (1-α) · partner`, α ∼ U(0.70, 0.85), partner = same Cold + different pseudo-speaker) instead of audio — pure tensor blend on cached pooled stats, sidesteps every audio-level artifact, ~1-2 min CPU for K=3 variants on 8532 chunks. **Revised gate framing for ALL cross-speaker augmentations:** the splice-detector hard threshold (≤ 0.55) was *always* wrong as a gate for any cross-speaker augmentation that injects partner information — by construction, augmented chunks contain partner content which is detectable. Detector UAR is *descriptive* (augmentation-class baseline detectability); the actual shortcut gate is **mix-bit-as-cold-predictor ≤ 0.52** (~0.5 expected by class-balanced augmentation: every chunk regardless of cold gets K=3 mixed variants, partner pool enforces same Cold, mix presence is uncorrelated with cold). The audit served its diagnostic purpose (Phase 3.5 caught the splicer-artifact problem cleanly) but not as the actual safeguard. **Paper framing — option 2 (audit-and-pivot as methodological contribution).** Don't bury the audio-splicing failure; lead with it. Draft methodology paragraph: *"We initially attempted audio-level cross-speaker splicing as the data-level de-confounding intervention. The splice-detector audit found UAR 0.998 — perfect detection. A self-splice control (splicing within the same pseudo-speaker cluster) gave UAR 0.990, revealing that the splicing operation itself, not the cross-speaker mixing, was the source of the artifactual signal. We pivoted to embedding mixup: a less aggressive but more reliable de-confounding mechanism that mixes WavLM pooled stats per chunk instead of audio. The audit-and-pivot itself is a transferable methodological warning — anyone doing audio-level cross-speaker augmentation on foundation-model representations should run a self-splice control."* **Generalises beyond URTIC.** Anyone doing audio-level cross-speaker augmentation for foundation-model-based health classification (WavLM/HuBERT/Whisper) should run a self-splice control before training. The artifact this caught isn't URTIC-specific — it's a property of how SSL speech models represent splice discontinuities at every layer. Transferable methodological warning worth flagging in any paper that uses audio-level augmentation on FM features.

**(M14) Speaker-probe-as-de-confounding-measurement has a fundamental noise floor on small-data corpora with coarse pseudo-speaker labels: discriminator memorisation (individual-chunk fingerprinting) dominates over generalising speaker structure on the substrate REGARDLESS of bottleneck, capping interpretable probe top-1 at ~ceiling × chance level.** ([plan.md §4.10.1.2](plan.md), [results/A7c_unbottlenecked.json](results/A7c_unbottlenecked.json)). After M10/M11/M12/M13 closed the projection-bottleneck case, we tested whether removing the bottleneck entirely (apply DANN directly at the 4096-d fused substrate) would reveal usable speaker signal. The M13 disc-ceiling pre-flight on the un-bottlenecked 4096-d substrate gave the same memorisation pattern as the 128-d case: D2 MLP 4096→1024→210 reached **best train top1 = 1.0000 with best devel top1 = 0.0837 (memo gap +0.9163)**; D3 MLP 4096→2048→1024→210 essentially identical (devel 0.0841, gap +0.9140). **Devel top-1 IS higher at 4096-d than at 128-d (0.083 vs 0.042) — the un-bottlenecked substrate retains more recoverable speaker info — but the memorisation gap is identical**, meaning ~91% of the discriminator's apparent train accuracy at both substrates is chunk-by-chunk fingerprinting rather than generalising speaker structure. **Adding capacity doesn't help: D2 ≈ D3 within noise**, so the ~9% devel ceiling is the substrate's actual limit on transferable speaker info, not a discriminator-architecture issue. The fundamental cause: with k=210 pseudo-speakers across 8.5k train chunks (~40 chunks per speaker), URTIC simply doesn't have enough per-speaker data to support generalising speaker prediction beyond ~9% top-1. **Implication for de-confounding measurement on small-data corpora.** Anyone using speaker probes as the de-confounding measurement instrument (the M3 / A5d / Suresh-style audit pattern) on a similar-scale corpus must: (a) report the disc-ceiling number alongside the probe number, (b) report the memorisation gap (train − devel) for each probe, (c) treat probe top-1 as an *upper bound* on speaker leakage rather than a precise measurement, since most of the apparent leakage may be chunk fingerprinting rather than generalising speaker structure. The "probe top-1 dropped from 0.07 to 0.04" framing common in the de-confounding literature is potentially misleading on small-data corpora — both numbers may sit at the substrate's memorisation noise floor and the change can be artefactual. **Generalises beyond URTIC.** Applies to any small-data paralinguistic corpus with k speakers and ~k coarse-cluster pseudo-speaker labels (most paralinguistic challenge corpora at the 5k-50k chunk scale fit this pattern). Pairs with M10 (bottleneck-confound), M11 (subtractive-objective), M12 (memorisation gap as in-flight check), M13 (disc-ceiling pre-flight ordering): together these five form the **speaker-probe-and-adversary instrumentation discipline** for representation-level de-confounding on foundation-model features in small-data paralinguistic settings. Each addresses a specific failure mode that's invisible without instrumentation.

**(M13, REFINED via A7c v1→v2 worked example) Disc-ceiling diagnostic ordering: run frozen-substrate discriminator-capacity ceiling BEFORE the adversarial λ-sweep — AND the decision logic must check `devel > HIGH threshold` BEFORE any memo-gap kill condition.** Memo gap is a regularisation warning (push the user toward dropout + weight decay + early stopping on the in-loop discriminator), not a fail-fast trigger. The A7c project provides the worked example: v1 used memo-gap-as-fail-fast and got `memorisation_dead` (skipping the λ-sweep entirely), but a reviewer correctly identified that devel = 0.084 cleared the 0.08 high threshold (~17.5× chance) — the substrate HAD transferable speaker signal; the huge memo gap was a regularisation issue (under-regularised discriminator memorising easily) not a "substrate dead" verdict. v2 corrected both axes (decision logic + regularised disc + per-epoch in-flight disc-devel tracking) and ran the full λ-sweep — which gave the cleaner `B_dann_dead_substrate_resistant` verdict (DANN got a fair fight and still didn't shape the substrate). The v2 verdict is genuinely categorical in a way v1's wasn't: at every measurable axis (cls UAR / MLP probe / LR probe / in-flight disc devel) the adversary effect is flat across λ ∈ [0.0, 0.1], so the closure isn't conditioned on any instrumentation question. **Lesson worth emphasising in the paper writeup:** `devel > HIGH` is the load-bearing condition for "substrate has signal worth pushing against"; memo gap is a separate axis that informs HOW to instrument the adversary (regularisation level), not WHETHER to run it. The two-axis check (signal capacity AND regularisation discipline) is the full M13 specification, not the original devel-only one. **(M13, original)**: ([plan.md §4.10.1.1](plan.md), [results/A7_disc_ceiling.json](results/A7_disc_ceiling.json)). ([plan.md §4.10.1.1](plan.md), [results/A7_disc_ceiling.json](results/A7_disc_ceiling.json)). The A7 PoC's in-loop discriminator was under-trained (lr 1e-4, 20 epochs, moving-target dynamics from the simultaneously-updating projection) and reached only 5× chance train accuracy. The B_null verdict-classifier correctly flagged this as uninterpretable. Both external reviewers independently recommended the same next step: freeze the substrate (λ=0 z), train a stronger discriminator at proper lr/horizon, see if it can recover speaker. Their recommendation IS the disc-ceiling diagnostic. **Methodological lesson:** because in-loop adversarial training has fragile dynamics (lr/horizon/sampling/target-motion all matter for whether the discriminator gets traction), it's nearly impossible to interpret an "adversary doesn't help" verdict without first establishing what a properly-trained discriminator on the frozen substrate could have achieved. Flipping the order — disc-ceiling first, λ-sweep second — separates substrate signal capacity (a property of the representation independent of the adversary's training) from adversarial dynamics (the GAN-style optimisation). Without the substrate-capacity number, an adversarial null is two findings entangled: "no signal in substrate" + "adversary couldn't capture signal even if it existed." With the substrate-capacity number, you can rule out one or both. **Generalises beyond URTIC.** Anyone running DANN/MDD/GRL-style adversarial training on a small-data low-dim representation should run the disc-ceiling diagnostic *first*; report it alongside any λ-sweep results; fail-fast on the substrate-no-signal case before spending compute on the λ-sweep at all. Pairs with M12 (memorisation gap as discriminator-validity check during the λ-sweep itself) — together they form the **adversarial-training instrumentation discipline**: M13 is the pre-flight check (substrate has signal at all?), M12 is the in-flight check (the adversary's discriminator is generalising rather than memorising?). Both are needed; together they make adversarial null results interpretable.

**(M12) Memorisation gap as discriminator-validity check for adversarial training: a speaker discriminator that hits high train accuracy via individual-chunk fingerprint memorisation but low devel accuracy provides no useful adversarial signal — the adversary "pushes against" speaker info that doesn't generalise to unseen chunks. Always check the train/devel gap before trusting an adversarial null result.** ([plan.md §4.10.1.1](plan.md), [results/A7_disc_ceiling.json](results/A7_disc_ceiling.json)). After A7 PoC's `B_null` verdict (discriminator at λ=0 trained to 0.0235, just below the 5×-chance threshold) flagged the adversarial verdict as uninterpretable, the disc-ceiling diagnostic loaded the saved A7 λ=0 projection checkpoints, froze them, and trained 3 progressively-stronger discriminators on `(z_train, spk_train)` evaluated on `(z_devel, spk_devel)`. **Result: even the strongest discriminator (MLP 128 → 1024 → 512 → 210, 200 epochs) reached only best devel top-1 = 0.0422 ± 0.0007 while best train top-1 = 0.9521 ± 0.0062 — a memorisation gap of +0.910.** Linear LR control: 0.0434 devel — within noise of the deepest MLP, confirming there's no nonlinear speaker structure for the deeper discriminators to find. **Mechanism reading**: the adversary in A7 wasn't pushing the projection away from a generalisable speaker direction; it was pushing against memorised in-batch fingerprints. That's why no λ_adv > 0 produced a real probe drop on devel even when the GRL was mechanically active (disc accuracy decreased monotonically with λ during training). **Generalises beyond URTIC.** Anyone running adversarial de-confounding (DANN/MDD/GRL-style) on a low-dim representation should: (1) run the matched-control λ=0 baseline (disambiguate "adversary helps" from "rest of pipeline helps" — the reflection point in §4.10.1's gate framing); (2) report discriminator train+devel accuracy throughout adversarial training (the adversary-health diagnostic from §4.10.1's per-epoch logging); (3) **establish the discriminator-capacity ceiling on the substrate via a frozen-substrate sweep over progressively stronger architectures and report the memorisation gap** (this finding). Without these three checks, an adversarial null result is uninterpretable. Together with M10 (bottleneck-confound) and M11 (subtractive-interaction), this completes the trio of **paper-relevant negative-control disciplines** for representation-level de-confounding on foundation-model features: M10 = "your apparent probe drop may be the bottleneck"; M11 = "your auxiliary contrastive loss may be subtractive"; M12 = "your adversarial null may be invalid because the discriminator memorises rather than generalises."

**(M11) Combining auxiliary objectives in low-data settings: subtractive interaction when one objective + the substrate already does the work the other was meant to provide.** ([plan.md §4.9.1.2](plan.md), [results/A6b_phase1_combined_lambda_sweep.json](results/A6b_phase1_combined_lambda_sweep.json)). After the controls (M10) showed cold-CE Pareto-dominates speaker-masked SupCon at head-only scope, the surviving hypothesis was *"contrastive as a regulariser on top of CE-anchored bottleneck might still help."* The λ-sweep `L = L_cold_CE + λ · L_supcon_speaker_masked` with λ ∈ {0.0, 0.05, 0.1, 0.25, 0.5} answered: **λ=0 is monotonically the best on every metric** — adding any positive λ strictly worsens MLP probe (0.0408 → 0.0462), LR probe (0.0371 → 0.0419), cold UAR (0.6128 → ~0.60), and class margin (+0.350 → +0.107). **Mechanistic explanation (4-step causal chain):** (1) CE on the 128-d projection bottleneck produces sharply class-separated geometry where speaker info is mostly compressed away by the bottleneck itself; (2) adding SupCon class-pressure flattens within-class diversity (CE already produced separation, contrastive adds no extra structure); (3) the flattened diversity includes cold-relevant variation that helped CE generalise — squeezing it out drops cold UAR; (4) SupCon treats "different cold class" as separation pressure regardless of speaker, so it pulls same-speaker different-cold pairs apart, *re-introducing speaker-correlated variance* and raising the MLP probe. **Naive intuition (wrong):** "CE for cold + SupCon for de-confounding = both gains stacked." **Empirical reality on URTIC + frozen WavLM at head-only scope:** subtractive — the contrastive objective competes with rather than complements the CE-shaped geometry, and the bottleneck has already done the de-confounding work the contrastive was supposed to add. **Generalises beyond URTIC.** In foundation-model + small-head paralinguistic setups generally, the "bottleneck does most of the work" pattern is probably common; a practitioner who reads M10/M11 and adds SupCon to a CE pipeline expecting de-confounding gains is doing the wrong thing. **Architectural argument against scaling up to layer-weight or full-fine-tune:** the failure mode is recipe-fundamental at any scope where CE anchors the optimization; opening more parameters gives the contrastive loss more to push around but doesn't change the underlying CE+bottleneck-already-compressed interaction. M10 + M11 together form a 2-finding pair on head-only contrastive de-confounding pitfalls: M10 = "your apparent probe drop may be the bottleneck, run a random-projection control"; M11 = "even if you add CE as the primary objective, contrastive on top is subtractive when the bottleneck already does the compression work." Both are paper-publishable as transferable methodological warnings in the foundation-model fine-tuning + paralinguistic-task literature.

**(M10) Probe-substrate dimensionality is itself a confound for de-confounding measurements; random-projection controls are mandatory for any rung that introduces a bottleneck.** ([plan.md §4.9.1.1](plan.md), [results/A6_phase1_PoC_controls.json](results/A6_phase1_PoC_controls.json)). The A6 head-only Phase 1 PoC reported a striking LR-probe drop from A2.5's 0.0725 to 0.0410 — apparent ~13σ de-confounding signal. **But the A2.5 baseline LR=0.0725 was measured on a 4096-d fused representation, while the A6 LR was measured on a 128-d L2-normalised projection output.** A 32× dimensionality reduction plus the unit-hypersphere metric change can drop LR-recoverable speaker information *independent of the training objective*. Three controls on the same 128-d L2-normalised z substrate decisively confirmed the confound: (C1) random untrained projection drops LR to 0.0427 — within 0.0017 of A6's 0.0410 (bottleneck does ~all of the apparent LR de-confounding); (C2) cold-CE only (no contrastive, no speaker-aware objective) gives LR 0.0373, MLP 0.0413, cold UAR 0.6124, margin +0.350 — Pareto-dominates A6 on every metric; (C3) vanilla SupCon (no speaker-masking) gives LR 0.0395, slightly *better* than A6's speaker-masked 0.0410 (the masking lever is mildly counterproductive). **Without the controls, we would have escalated to Phase 2 (~2-4 hr GPU) on a false-positive belief that the contrastive recipe was de-confounding the representation.** The controls (~1.6 min) caught it. **Generalises beyond URTIC.** Anyone doing representation-level de-confounding on foundation-model features with a projection MLP should run a random-projection control at the same target dim before claiming mechanism activation; substrate-specific absolute thresholds calibrated against a high-dim baseline can fire false-positive de-confounding verdicts. **Transferable methodological warning** worth flagging in any paper that uses representation-level de-confounding via dimensionality-reducing projection. Pairs naturally with M8 (audio-splice artefact) and M9 (mixup narrow-window-empty) as the *third* paper-relevant negative-control discipline: M8 = self-splice control disambiguates "splicing op" from "cross-speaker mixing"; M9 = α-endpoint control closes the data-level operating window; M10 = random-projection control disambiguates "bottleneck" from "training objective" at the representation level.

**(M9) Embedding mixup on URTIC's WavLM has an EMPTY operating window across the α-axis: conservative α ∈ [0.70, 0.85] is too gentle to de-confound (probes flat); aggressive α ∈ [0.50, 0.70] damages label validity (UAR drops, σ explodes) without recovering de-confounding. Both endpoints fail; intermediate ranges cannot escape.** *(Extended after the aggressive-α α-sweep landed branch (d): see [results/A5_5_planB_phase4_mixup_aggro.json](results/A5_5_planB_phase4_mixup_aggro.json) — UAR 0.6397 ± 0.0186 [Δ -0.017 vs A2.5, σ exploded 6×], MLP probe 0.0485, LR probe 0.0735, cos(init, final) = 0.99998 across all 3 seeds [EVEN MORE locked at A2.5 init], best_epoch = {1, 1, 1}. The branch (d) verdict closes the α-axis: branches a/b/c would have left "try another α" open; branch (d) shows both endpoints fail differently. Intermediate U(0.60, 0.80) can't escape — gentler falls toward conservative's null, stronger toward aggressive's UAR damage. **Mechanism (post-aggressive-α):** mixup on post-frozen-backbone representations cannot de-confound without an explicit objective; the augmented pooled vectors either look statistically indistinguishable from natural ones (no de-confounding signal for the model to learn) or look noisy enough to corrupt the cold label (UAR damage). The de-confounding load belongs at the representation level (A6 contrastive, speaker-masked positives) or gradient level (A7 MDD/DANN adversary), not the data level. **Transferable methodological warning (revised):** for any cross-speaker augmentation on foundation-model representations operating purely at the data level (without an explicit de-confounding loss), the operating window may be narrow OR EMPTY. Test both endpoints before claiming the augmentation works; a single-α verdict can read as "modest contribution + null on probe" without revealing whether the α-axis has any usable point at all.)*

**(M9 — original conservative-α writeup, retained for completeness)** Embedding mixup at safe α (∈ [0.70, 0.85]) on URTIC's WavLM produces a small UAR lift but does NOT activate the speaker-de-confounding mechanism — the layer-weight subspace stays at A2.5's converged state. ([plan §4.8.6 / §4.8.8](plan.md), [results/A5_5_planB_phase4_mixup.json](results/A5_5_planB_phase4_mixup.json)). Plan B Phase 4 trained the A5.5 head on K=3 mixed pooled-stats variants per chunk (α ∼ U(0.70, 0.85)), warm-started from A2.5, 3 seeds {42, 123, 7}, devel unaugmented, identical training recipe to A2.5. **Result: UAR 0.6624 ± 0.0031 (Δ +0.006 vs A2.5 0.6564 ± 0.0038, ~1.6σ above zero); MLP probe top-1 0.0506 ± 0.0019 (Δ +0.0005 vs A2.5 0.0501 — essentially unchanged); LR probe top-1 0.0759 ± 0.0030 (Δ +0.003 vs A2.5 0.0725 — slightly UP, within noise); val_test_gap -0.0291 (more pessimistic than A2.5's -0.020).** **Critical diagnostic: cos(init_A2.5, final) = 0.9999, 1.0000, 0.9997 across all 3 seeds — the optimizer DID NOT MOVE the layer weights from A2.5's converged state.** Best epochs are early (3, 1, 5). The +0.006 UAR comes entirely from MLP/classifier-level adjustments to the augmented training distribution, NOT from layer-weight refinement (consistent with M5's flat-landscape-at-default-lr finding). **3-D acceptance gate: gate 1 (UAR floor 0.6525) PASS by +0.010; gate 2a (MLP probe drop ≤ 0.0456) FAIL (probe slightly UP); gate 2b (LR probe drop ≤ 0.0723) FAIL (probe slightly UP); gate 3 (mix-bit shortcut) PASS by construction. Overall: FAIL on strict 3-D gate.** Diagnosis: α=0.775 mean = 22.5% partner contribution is too gentle to push the model toward speaker-invariance — the audit detector at 0.503 confirms mixed pooled vectors are statistically indistinguishable from natural ones, so the model has nothing to "learn invariance to." **Recall-pattern observation worth reporting beyond the UAR scalar:** A2.5 was heavily NC-biased (recC 0.43, recNC 0.87); A5.5 mixup is more balanced (recC 0.47, recNC 0.85). For minority-class detection, recovering more cold examples is practically valuable even when net UAR moves only +0.006. **Symmetric failure-mode framing for the paper:** audio splicing was "too detectable to use" (UAR 0.998 detector → shortcut concern) — embedding mixup at conservative α is "too undetectable to teach" (UAR 0.503 detector → no signal for the model to learn invariance to). The α-sweep (plan §4.8.8, queued, ~30 min) tests whether there's a Pareto-clean middle ground at α ∈ [0.5, 0.7] (35-50% partner contribution): (a) UAR holds + probe drops → strict PASS; (b) UAR holds + probe stays → confirmed limitation, lock as "modest UAR / null on probe" + move to A6; (c) UAR drops + probe drops → Pareto trade-off documented. **Transferable observation:** for any cross-speaker augmentation on foundation-model representations, the augmentation strength must be high enough to produce *measurably* different representations (else the model has no de-confounding signal) but low enough to preserve label validity (else cold prediction degrades). Both extremes fail; the operating window may be narrow or empty depending on the corpus + backbone.

**(A4) A5.5 — embedding-mixup data-level augmentation (LOCKED at conservative α ∈ [0.70, 0.85] as canonical; aggressive α ∈ [0.50, 0.70] ships as ablation evidence that closes the α-axis).** ([plan §4.8.5 / §4.8.6 / §4.8.8](plan.md), [results/A5_5_planB_phase2_mixup.json](results/A5_5_planB_phase2_mixup.json), [results/A5_5_planB_audit.json](results/A5_5_planB_audit.json), [results/A5_5_planB_phase4_mixup.json](results/A5_5_planB_phase4_mixup.json), [results/A5_5_planB_phase2_mixup_aggro.json](results/A5_5_planB_phase2_mixup_aggro.json), [results/A5_5_planB_phase4_mixup_aggro.json](results/A5_5_planB_phase4_mixup_aggro.json)). Pivot from the audio-splicing v1 (killed by M8 self-splice diagnostic). **Recipe**: mix WavLM pooled stats per chunk (`α · anchor + (1-α) · partner`, partner = same Cold + different pseudo-speaker, K=3 variants per chunk). Pure CPU tensor op on cached pooled stats; ~1-2 min per cache build. **Audit (both gates PASS, α-independent)**: detector UAR 0.5034 (chance, Δ -0.4947 vs Phase 3 audio splicing); mix-bit-as-cold 0.5000 (trivial PASS by class-balanced construction). **Two α variants tested:**

| variant | α range | UAR | Δ vs A2.5 | MLP probe | LR probe | cos(init,final) | 3-D gate verdict |
| ------- | ------- | --- | --------- | --------- | -------- | --------------- | ---------------- |
| **conservative (LOCKED)** | U(0.70, 0.85) | 0.6624 ± 0.0031 | +0.006 (~1.6σ) | 0.0506 (~unchanged) | 0.0759 (+0.003) | ~0.9999 | PARTIAL PASS (UAR floor PASS; probe drop FAIL) |
| **aggressive (ABLATION)** | U(0.50, 0.70) | 0.6397 ± 0.0186 | **-0.017 (σ 6×)** | 0.0485 (within noise) | 0.0735 (within noise) | 0.99998 (more locked) | branch (d): UAR drops AND probe doesn't drop |

**Lock decision:** conservative α as canonical A5.5 (the only variant with positive UAR contribution and clean recall-balance shift recC 0.43 → 0.47); aggressive α as the ablation row that closes the α-axis (M9 narrow-window-empty). **Mechanism**: at conservative α, the +0.006 UAR is MLP/classifier-level regularization from training-data variation (cos(init, final) ≈ 1, layer weights didn't move); at aggressive α, label-validity damage from partner-dominant mixes shows up in UAR (σ explodes 6×, best_epoch collapses to {1, 1, 1}) before any de-confounding shows up in the probe. **Paper framing — A5.5 in the 3-level de-confounding ladder:** A5.5 = data-level rung; A6 = representation-level (contrastive with speaker-masked positives, explicit speaker-invariance objective); A7 = gradient-level (MDD/DANN adversary). A5.5's modest contribution + null-on-probe is itself the load-bearing evidence for *why* A6 and A7 are architecturally necessary — data-level intervention without an explicit de-confounding objective cannot break the speaker shortcut on URTIC's frozen WavLM. Paper claim: *"Embedding mixup at conservative α (∈ [0.70, 0.85]) gave a small UAR lift over A2.5 (+0.006, within σ) with a more cold-balanced recall pattern; the speaker probe did not drop. An aggressive α-sweep (∈ [0.50, 0.70]) confirmed the operating window for per-chunk pooled-stat mixing on URTIC's frozen WavLM is empty: UAR dropped (Δ -0.017) without any probe drop, ruling out narrower-but-real settings. The de-confounding load moves to A6 (representation-level contrastive) and A7 (gradient-level adversary)."*

**(A4-old) A5.5 — embedding-mixup data-level augmentation (PARTIAL PASS at α ∈ [0.70, 0.85]; α-sweep follow-up queued).** ([plan §4.8.5 / §4.8.6 / §4.8.8](plan.md), [results/A5_5_planB_phase2_mixup.json](results/A5_5_planB_phase2_mixup.json), [results/A5_5_planB_audit.json](results/A5_5_planB_audit.json), [results/A5_5_planB_phase4_mixup.json](results/A5_5_planB_phase4_mixup.json)). Pivot from the audio-splicing v1 (killed by M8 self-splice diagnostic). **Recipe**: mix WavLM pooled stats per chunk (`α · anchor + (1-α) · partner`, α ∼ U(0.70, 0.85), partner = same Cold + different pseudo-speaker, K=3 variants per chunk). Pure CPU tensor op on cached pooled stats; ~1-2 min for the full 8532-chunk × K=3 cache build. **Audit (both gates PASS)**: detector UAR 0.5034 (chance, Δ -0.4947 vs Phase 3 audio splicing); mix-bit-as-cold 0.5000 (trivial PASS by class-balanced construction). **Phase 4 training (3 seeds, warm-start from A2.5)**: UAR 0.6624 ± 0.0031 (Δ +0.006 vs A2.5, ~1.6σ); recall pattern more cold-balanced (recC +0.04, recNC -0.02); MLP probe ~unchanged (0.0506 vs 0.0501); LR probe slightly UP (0.0759 vs 0.0725, +0.003 within noise). cos(init, final) = 0.9999 — optimizer didn't move from A2.5's converged state. **3-D gate verdict: PARTIAL PASS** (UAR floor PASS, speaker probe drop FAIL on strict gate; mix-bit shortcut PASS by construction). **Mechanism**: the +0.006 UAR is MLP/classifier-level regularization from training-data variation, not layer-weight refinement; the de-confounding mechanism didn't activate at conservative α. **Status**: α ∈ [0.5, 0.7] sweep queued (§4.8.8) to test whether more aggressive mixing activates the de-confounding signal; either lock the aggressive variant as canonical A5.5 (strict PASS) or accept the conservative result and move to A6 (where contrastive loss provides stronger built-in de-confounding via speaker-masked positives).

### 14.2 Architectural contributions

**(A1) A2.5 — honesty-prior layer-weight init (CANONICAL, locked)** ([plan §4.6 / §4.6.1 / §4.6.2](plan.md), [results/A2_grouped_honestprior.json](results/A2_grouped_honestprior.json), [results/A2_lr_init_grid.json](results/A2_lr_init_grid.json), [results/A2_grouped_honestprior_lr_stress.json](results/A2_grouped_honestprior_lr_stress.json)). Initialise WavLM layer-weight softmax logits = T·sub@1 from the per-layer audit (M3). Result on grouped splits at default lr: **UAR 0.6564 ± 0.0038, +0.020 (4.7σ) over uniform-init A2_grouped**; MLP probe flat (0.0501 → 0.0501); LR probe slightly down (0.0760 → 0.0725). Pareto improvement on every measured dimension. **Canonical anchor for downstream rungs.**

**Mechanism (third-pass revision; final paper claim):** the original "flat landscape" claim was wrong (the lr stress test showed cos(init, final) drops from 0.9999 to 0.21 across lr×{0.1, 1, 10, 100}). The first revision said "good init dominates because lr too low to move." The lr × init grid (18 retrains, uniform vs honest_prior crossed with lr×{3, 5, 10}, 3 seeds) revises that further: **uniform init plateaus at UAR 0.6447–0.6466 across all tested lr — never reaches the 0.656+ that honesty-prior achieves at every lr setting**. Δ(prior − uniform) at lr×10 = +0.019 with prior having tighter σ (0.0034 vs 0.0070). **The honesty prior is a distinct attractor in the layer-weight subspace that uniform init cannot reach via lr alone.** Strongest formulation:

> *"Data-derived layer-weight init produces +0.020 UAR over uniform init at default lr; the contribution is not 'cheaper start to the same attractor' but rather convergence to a distinct attractor that uniform init does not reach even with 10× layer-weight learning rate. The optimizer at default lr essentially stays at the prior (cos(init, final) = 0.9998); at higher lr it can move (cos drops to 0.83 at lr×10) but lands at the same UAR plateau the prior already provides — and uniform init never reaches that plateau via lr alone. The audit's per-layer signal informs an architectural prior that's a genuine basin of attraction, not a low-lr training artifact."*

**Speaker probe trade-off documented.** Higher lr inflates LR speaker probe (0.0725 → 0.088 at lr×10) — the optimizer concentrates weight on early speaker-rich layers (top-5 shifts toward L0/L4/L5). For the speaker-honesty paper narrative, A2.5-default (honest_prior + lr×0.1) is Pareto-canonical: same UAR as honest_prior + lr×3 (+0.0008) with lower probe leak. honest_prior + lr×10 trades +0.007 UAR for +0.016 probe inflation — bad trade. A2.5-default stays canonical; lr × init grid serves as ablation evidence that the prior contribution survives across lr regimes.

**(A2) Logit-level late fusion with honesty-fixed admission (A5b) — LOCKED PASS at +0.035 over A2.5** ([plan §5.3 / §4.7 / §4.7.3](plan.md)). Per-group cold-probe logits combined as `final_logit = logit_A2 + β · mean_g(zscore_g(logit_g))`, hard top-K admission by subtractive-honesty `sub@1 = lab_gain - speaker_gain`. **Three anchor results, four protocol variants:**

- **(A2-a) On uniform-A2 anchor (random splits, β=1.00 forced)** ([results/A5b.json](results/A5b.json)): K=1 free-sweep FAIL (UAR 0.6502 ± 0.0078, σ > effect size); K-locked K=1 PASS at UAR 0.6576 ± 0.0011, Δ +0.0148 (3.3σ vs A2_argmax). The K-sweep pathology (M4) drove the FAIL; locking K removed the over-fit channel.
- **(A2-b) On uniform-A2_grouped anchor (β=1.00 forced)** ([results/A5b_grouped.json](results/A5b_grouped.json)): K=1 PASS at UAR 0.6588 ± 0.0059, Δ +0.0227 (3.8σ vs A2_argmax_grouped). Lift larger than (A2-a) because A2_argmax_grouped is the leak-corrected baseline.
- **(A2-c) On A2.5 anchor with β=1.00 forced** ([results/A5b_grouped_honestprior.json](results/A5b_grouped_honestprior.json)): FAIL (Δ -0.022, τ pegged at search edge). Diagnosed as **calibration failure under wrong-anchor β** — β=1.00 was implicitly conditioned on uniform-A2.
- **(A2-d) On A2.5 anchor with β-sweep (LOCKED)** ([results/A5b_grouped_honestprior_betasweep.json](results/A5b_grouped_honestprior_betasweep.json), [results/A5b_grouped_honestprior_betasweep_extended.json](results/A5b_grouped_honestprior_betasweep_extended.json)): β-sweep over {0..2.0} → PASS at boundary β*=2.0 (UAR 0.6755, Δ +0.019); extended sweep over {2.5..16} → **interior peak at β=6, plateau β ∈ [4, 16]**. Per-seed argmax-locked β* = [6, 8, 4] (none at boundary); **K=1 fused UAR 0.6913 ± 0.0076, Δ +0.0349 ± 0.0054 (~6.5σ, strong PASS)**. Robust-β diagnostic: smallest β within 1σ of peak = β=4. G4-alone (β=∞) hits 0.6632 ± 0.0000; K=1 fused beats it by +0.028 — fusion is genuinely additive. **Three reportable lock options:**

  | Lock                                        | β                | UAR             | Δ vs A2.5       |
  | ------------------------------------------- | ---------------- | --------------- | --------------- |
  | Per-seed argmax (protocol-strict, headline) | per-seed [6,8,4] | 0.6913 ± 0.0076 | +0.0349 (~6.5σ) |
  | Aggregate-peak (across seeds)               | 6.0              | 0.6917 ± 0.0085 | +0.0353         |
  | Robust-β (across seeds, conservative)       | 4.0              | 0.6875 ± 0.0111 | +0.0311         |

**Speaker-probe gates PASS in all four variants** (β-independent: probe (i) and (ii) operate on β-independent feature spaces). Locked values: probe (i) literal 2-D = 0.0156 ± 0.0026 (≪ 0.078 ceiling, ~5× margin); probe (ii) backbone-concat = 0.0733 ± 0.0002 (≤ 0.078 ceiling, by 0.005). The honesty-audit value can be capitalised via the **architectural prior route** (A1) AND the **fusion route** (A2) — they capture *partially overlapping* signal, but fusion adds genuinely new lift on top of the prior (+0.035 at locked β). **Final paper claim:** *"A2.5 prior + K=1 fusion are two clean stackable contributions: A2.5 lifts +0.020 over uniform A2 baseline, K=1 fusion lifts a further +0.031 to +0.035 over A2.5 (β=4-6 plateau, robust to grid choice). G4-alone (a 7-dim handcrafted feature) reaches 0.6632 alone, comparable to A2.5 (0.6564), but the fused K=1 PASSES both UAR (~6.5σ above gate) and speaker-probe gates by wide margins — confirming partial orthogonality of A2.5 and G4 cold signals."*

**(A3) Per-channel cold-probe as speaker-information bottleneck (architectural validation)** ([EXPLAINER §10](EXPLAINER.md), [plan §5.7](plan.md)). Locked-K speaker probes show ~5.5 pp drop between upstream concat (`pooled_4096 ⊕ G4_gi_7`, top-1 ≈ 0.073) and fusion-input (`[logit_A2, z_logit_G4_gi]`, top-1 ≈ 0.012). The per-channel cold-MLP that compresses 4096-d → 1-d cold logit is the actual bottleneck — speaker info correlated with cold gets retained, speaker info orthogonal to cold gets stripped. This is the clean architectural justification for choosing logit-level fusion over A3's concat-MLP, and explains how A2.5's prior can pull from both early speaker-rich layers (L0/L2/L5) AND late cold-pure layers (L22/L24) without inflating speaker leak. **Holds across all four (A2) variants** — the bottleneck mechanism is split-protocol-independent, anchor-independent, and β-independent (probes don't see β).

**(A2) Logit-level late fusion with honesty-fixed admission (A5b)** ([plan §5.3 / §4.7](plan.md)). Per-group cold-probe logits combined as `final_logit = logit_A2 + β · mean_g(zscore_g(logit_g))`, hard top-K admission by subtractive-honesty `sub@1 = lab_gain - speaker_gain`. **Two anchor results, three protocol variants:**

- **(A2-a) On uniform-A2 anchor (random splits, β=1.00 forced)** ([results/A5b.json](results/A5b.json)): K=1 free-sweep FAIL (UAR 0.6502 ± 0.0078, σ > effect size); K-locked K=1 PASS at UAR 0.6576 ± 0.0011, Δ +0.0148 (3.3σ vs A2_argmax). The K-sweep pathology (M4) drove the FAIL; locking K removed the over-fit channel.
- **(A2-b) On uniform-A2_grouped anchor (β=1.00 forced)** ([results/A5b_grouped.json](results/A5b_grouped.json)): K=1 PASS at UAR 0.6588 ± 0.0059, Δ +0.0227 (3.8σ vs A2_argmax_grouped). Lift larger than (A2-a) because A2_argmax_grouped is the leak-corrected baseline.
- **(A2-c) On A2.5 (honesty-prior) anchor** ([results/A5b_grouped_honestprior.json](results/A5b_grouped_honestprior.json), [results/A5b_grouped_honestprior_betasweep.json](results/A5b_grouped_honestprior_betasweep.json)): β=1.00 forced FAIL (Δ -0.022, τ pegged at search edge -3.95) → diagnosed as **calibration failure under wrong-anchor β**, not signal absence (β=1.00 was implicitly conditioned on uniform-A2). β-sweep over {0..2.0} jointly with τ recovers PASS at β*=2.0 (all 3 seeds at upper boundary), K=1 fused UAR 0.6755 ± 0.0208, Δ +0.019 ± 0.018 (PASS gate by mean, ~0.7σ above gate threshold). β=0 sanity reproduces A2.5_τ exactly. **Caveat: β* boundary-pegged** — same diagnostic family as M4. Per-seed: 1/3 neutral, 2/3 lift. Recall pattern at β*=2.0 (recC 0.82, recNC 0.53) qualitatively different from A2.5's balanced 0.45/0.86 — high-β fusion may be mechanism-different from "stack G4_gi onto A2.5." Final lock pending extended β grid {2.5, 3, 4, 6, 8, 12, 16} + G4-alone baseline (§4.7.2 + §4.6.2 controls).

**Speaker-probe gates PASS in all three variants.** The honesty-audit value can be capitalised via the **architectural prior route** (A1) OR the **fusion route** (A2), with the prior route giving cleaner statistical support and a simpler training stage. The two routes capture overlapping signal — the fusion lift on top of A2.5 (Δ +0.019) is comparable in magnitude to the fusion lift on top of uniform-A2 (Δ +0.023), but the honesty-prior baseline already absorbs ~half the signal that uniform-A2 was missing. **Two-stackable-contributions framing tentatively supported but caveated;** the cleaner paper claim is "the audit produces useful architectural priors AND is competitive as a fusion-stage admission rule; both routes deliver paper-comparable lift."

**(A3) Per-channel cold-probe as speaker-information bottleneck (architectural validation)** ([EXPLAINER §10](EXPLAINER.md), [plan §5.7](plan.md)). Locked-K speaker probes show ~5.5 pp drop between upstream concat (`pooled_4096 ⊕ G4_gi_7`, top-1 ≈ 0.073) and fusion-input (`[logit_A2, z_logit_G4_gi]`, top-1 ≈ 0.012). The per-channel cold-MLP that compresses 4096-d → 1-d cold logit is the actual bottleneck — speaker info correlated with cold gets retained, speaker info orthogonal to cold gets stripped. This is the clean architectural justification for choosing logit-level fusion over A3's concat-MLP, and explains how A2.5's prior can pull from both early speaker-rich layers (L0/L2/L5) AND late cold-pure layers (L22/L24) without inflating speaker leak. **Holds across all three (A2) variants** — the bottleneck mechanism is split-protocol-independent and anchor-independent.

### 14.3 Negative results (publishable)

**(N1) Phoneme-CTC labelling on URTIC fails (CommonVoice domain mismatch)** ([summary.md "Path 1 ABANDONED"](summary.md)). 84% blank-wins-top1 on URTIC (vs ~38% silence in manner labels), mean per-frame entropy 0.16 nats — sharply confident, not smeared. Smearing heuristics rejected as untestable (no phoneme-boundary ground truth on URTIC). Documented with full diagnostic.

**(N2) Manner-aware pooling (A3) reintroduces speaker shortcut even with clean labels** ([plan §4.2](plan.md), [results/A3.json](results/A3.json)). pYIN+RMS 3-cat manner labels are clean (validated on subset), but per-(layer, manner-cat) pooled WavLM stats fed to a concat-MLP head place all weight on early WavLM layers (L1/L4/L8 — where speaker info lives) and inflate the speaker probe (top-1 0.0501 → 0.0555). Per-utterance per-category mean of WavLM frames in early layers IS a speaker fingerprint. Rejected as a representation stream; manner labels survive as conditioning info for A5 scalars.

**(N3) OOD Mahalanobis distance as a cold feature is anti-predictive on URTIC** ([summary.md A5a](summary.md), [results/A5a_honesty.csv](results/A5a_honesty.csv)). The original PDF-A5 hypothesis ("distance from non-cold manifold predicts cold") gives UAR 0.4334 (label_gain -0.067). Cold lives near the middle of the non-cold manifold in the dimensions A2 cares about. Documented; G8 dropped from admission pool but kept as a row in the honesty table.

**(N4) WavLM-base-plus-sv cannot recover speaker structure on URTIC (pseudo-speaker negative control)** ([summary.md pseudo-speaker validation](summary.md)). KMeans-vs-HDBSCAN ARI = 0.093 on WavLM-SV embeddings (vs 0.856 on ECAPA-VoxCeleb). Justifies the architectural-independence choice (ECAPA + KMeans for pseudo-speakers, not a WavLM-derived encoder) empirically.

### 14.4 Open items to resolve before the paper

**DONE since last revision** (controls landed and verdicts locked):

- **(DONE) A2 lr × init grid** — 18 retrains landed (`results/A2_lr_init_grid.json`). **Verdict: A2.5-default (honest_prior + lr×0.1) stays canonical.** The honesty prior is a *distinct attractor* that uniform init does not reach via lr alone — uniform plateaus at 0.6447–0.6466 across lr×{3, 5, 10}; honesty-prior reaches 0.657–0.664 at every lr. Higher lr trades +0.007 UAR for +0.016 LR-probe leak (Pareto-dominated by A2.5-default for the de-confounding paper narrative). See (A1) above.
- **(DONE) A5b extended β-sweep + G4-alone baseline** — extended grid {2.5..16} landed (`results/A5b_grouped_honestprior_betasweep_extended.json`). **Verdict: A5b K=1 LOCKED PASS at +0.035 over A2.5.** Per-β UAR climbs sharply β=2 → β=4, plateaus at 0.6875–0.6917 across β ∈ [4, 16], drops to 0.6632 at β=∞. Interior optimum at β=6 (peak), robust-β at β=4. Per-seed argmax β* = [6, 8, 4] (none at boundary). G4-alone reference 0.6632 — striking parity with A2.5-alone (M7). See (A2-d) above.

**Tracked (to settle before paper submission):**

- **5-seed re-run on the locked canonical A2.5** — current σ_argmax = 0.0038 at N=3; the val-test gap widens with higher lr across both inits (uniform: -0.020 → -0.018; prior: -0.022 → -0.022 across lr×{3, 5, 10}). A2.5-default at lr×0.1 has the cleanest gap (-0.020). 5 seeds will tighten the verdict-stability claim before paper submission.
- **A5b §4.11.1.5 K=3 G_egemaps_full (DONE) — NO ADMIT, K=2 stays canonical, A5a slicing methodology validated; standalone-UAR-predictor heuristic confirmed across 6 candidates.** (`results/A5b_k3_egemaps_5seed.json`, 2.23 min, 5 seeds.) Two configs tested: **Config A** (K=2 with G_egemaps_full replacing G5) → **0.6692 ± 0.0056** (Δ -0.0345 vs K=2 LOCKED, per-seed locked β* mostly-boundary [16, 16, 8, 16, 12]). **Config B** (K=3 = A2.5 + G4_gi + G5_mod + G_egemaps_full, the real question) → **0.6801 ± 0.0097** (Δ -0.0236 vs K=2 LOCKED, β* boundary-heavy [16, 12, 16, 16, 8]). Neither beats K=2 LOCKED canonical (0.7037 ± 0.0060). G_egemaps_full standalone cold-LR UAR = 0.5384 (chance + 0.04, deterministic across seeds because LR is convex). **Standalone-UAR-predictor heuristic now confirmed across 6 candidates** with a clean ~0.61 threshold partitioning K-fusion admit (G5_mod 0.6121 WIN, G1_voicing 0.6058 borderline) from fail (G6 0.6053 FAIL, eGeMAPS_full 0.5384 FAIL, G2 0.5088 FAIL, G3 0.5039 FAIL); all failed cases pin β* at boundary 12-16 with τ at negative extreme — same calibration-absorbed pattern. **Mechanism reading — why eGeMAPS_full fails despite containing G3+G6+53-other dims:** the full 88-d set wraps audited G3 (14-d voice quality) + G6 (21-d spectral) PLUS 53 un-extracted dimensions (energy/loudness functionals, F0/HNR aggregates, MFCC stats, spectral-tilt summaries). The cold-LR probe (LogReg, C=1.0) on 8.5k chunks × 88 features spreads weight across many cold-irrelevant dims; standalone UAR (0.5384) is WORSE than G6 alone (0.6053) — adding the un-extracted 53 dims actively HURTS discriminability, the dimensionality dilution outweighs any extra signal. **Paper-stage validation finding (the prediction list's "K=3 fails" branch fired):** *"A5a's curated slicing into G3 (voice quality) + G6 (spectral) extracted essentially all of eGeMAPS's cold-relevant content. The full 88-d set scored worse than the G6 subset alone (0.5384 vs 0.6053 standalone cold UAR) and failed both K=2 (replacing G5) and K=3 (adding to K=2) fusion gates. The audit-driven dimensional carving was load-bearing methodology, not cosmetic."* Validates the M3 honesty-audit framing — the slicing decisions weren't decoration. **Standalone-UAR heuristic worth a paper paragraph:** *"For honesty-audited late-fusion stacks on small-data paralinguistic corpora, K-fusion candidate admission is well-predicted by standalone cold-LR UAR; we observed a clean threshold around 0.61 above which candidates fuse productively (interior β*) and below which they get calibration-absorbed at boundary β* with τ at extreme. Saves β-sweep compute on candidates that won't admit."* **A5b K=2 = FINAL canonical late-fusion system, exhaustively validated.** All 6 A5a-admitted groups + the full eGeMAPS_full superset tested as K=2 partners or K=3 additions; no configuration beats K=2 LOCKED. K=2 5-seed mean-logit ensemble (0.7090) remains the best controlled-system paper-headline number. Distance to 0.71 baseline: 0.001. **Next reviewer-recommended axis: structurally different FM (HuBERT/wav2vec2 pooled stats as a second backbone)** — only A5b axis left to push past 0.7090.
- **A5b K=2 5-seed MEAN-LOGIT ENSEMBLE (DONE; Tier-1 §4.11.1.4) — 0.7090 on devel_test, distance to 0.710 baseline = 0.001 (within measurement noise).** (`results/A5b_k2_5seed_ensemble.json`, 0.89 min.) Per-chunk linear average of the 5 per-seed K=2 fused logits at each seed's locked β* ∈ {6, 8, 8, 8, 12}, then τ swept fresh on train_threshold for the ensemble. **MEAN-LOGIT 0.7090** (+0.0053 over per-seed single mean 0.7037 ± 0.0060). MEAN-PROBABILITY 0.7041 (+0.0004, essentially neutral). **Per-seed reproduction sanity check:** the ensemble cell recomputes per-seed K=2 single UARs at locked β*/τ* and reproduces the §4.11.1.2 numbers exactly (0.6942, 0.7094, 0.7032, 0.7083, 0.7035 for seeds {42, 123, 7, 999, 31337}) — confirms the per-seed locked-β* + cached-features pipeline is fully deterministic. **Mechanism — why mean-logit beats mean-probability:** per-seed fused logits have similar magnitudes (β*'s within 2× of each other, all τ*'s in [-3.95, +1.275]) so linear averaging in logit space preserves the ranking signal across chunks. Mean-probability's nonlinear sigmoid pooling washes out information when the per-seed logit magnitudes carry confidence — taking sigmoid first throws that away. **Recall pattern flip — cold-balanced ensemble.** Ensemble recC = 0.791 / recNC = 0.627 (heavily cold-balanced) vs per-seed single typically recC ≈ 0.43 / recNC ≈ 0.87 (NC-biased). Averaging 5 different operating points (each with its own τ*) shifts the effective decision toward higher cold recall. **For a 9.5%-cold corpus, recovering 79% of cold cases is practically valuable beyond what the UAR scalar captures** — paper should report both UAR + recall pattern shift between per-seed single and ensemble. **Cumulative final state:** uniform-A2-grouped 0.6361 → A2.5 0.6563 (+0.020) → K=1 0.6934 (+0.037 over A2.5) → K=2 single 0.7037 (+0.010 over K=1) → **K=2 5-seed mean-logit ensemble 0.7090 (+0.005 over single, +0.073 over baseline, distance to 0.710 = 0.001)**. **Paper headline framing options:** conservative ("approaches 0.710 within 0.001 UAR; matches within rounding"), standard ("matches 0.710 within measurement noise"), generous ("matches 0.710 in rounded reporting at 0.71 precision"). **Caveats (paper-stage):** (1) ensemble UAR is single-number not σ-bearing — σ-bearing canonical stays per-seed K=2 single mean (0.7037 ± 0.0060); ensemble is "ablation row showing ensemble averaging buys +0.005 UAR." (2) devel_test ≠ hidden test — the 0.710 baseline was hidden-test in 2017; we report 0.7090 on devel_test under speaker-grouped CV (apples-to-oranges, paper will include both with explicit disclaimer). (3) τ swept on train_threshold for the ensemble (proper protocol, no multiple-comparison-on-devel). (4) Ensemble adds 5× inference cost — paper should report per-seed single (cheap, σ-bearing) AND ensemble (expensive but +0.005 lift).
- **A5b K=2 SPEAKER PROBES (DONE; Tier-1 §4.11.1.3) — both gates PASS at 5 seeds, A5b K=2 FINAL canonical late-fusion system locked.** (`results/A5b_k2_5seed_speaker_probes.json`, 1.47 min.) Two probe substrates per the K=1 PASS protocol (plan §4.7): probe (i) LITERAL = 3-d fusion-input vector `[logit_A2.5, z_logit_g4_gi, z_logit_g5_modulation]` (what the τ-comparator sees); probe (ii) BACKBONE-CONCAT = 4167-d substrate `pooled_4096 (A2.5 layer-weighted-fused) ⊕ G4_gi (7) ⊕ G5_modulation (64)` (the underlying representation that produces the logits). Multinomial LR via `honesty.speaker_probe`; gate ceiling = A2 LR-grouped + 1σ = 0.0780. **Results: probe (i) 0.0182 ± 0.0006 (4.3× margin under gate); probe (ii) 0.0729 ± 0.0005 (Δ 0.0051 under gate). Both PASS at 5 seeds.** Per-seed σ tiny (0.0005-0.0006) — highly reproducible. K=2 vs K=1 marginal probe increases (+0.0063 literal, +0.0054 backbone-concat) are *capacity-driven* (3-d > 2-d; 4167-d > 4103-d) not *speaker-leak-driven* — neither substrate has been shaped by speaker-aware training, so the small increase per added dimension is the LR probe finding minor extra separation capacity in the wider input space, not the K=2 fusion design leaking. **Exploratory-vs-confirmatory framing (paper methodological honesty):** the K=2 G_other selection (§4.11.1.1) tested 5 candidates {G1, G2, G3, G5, G6} on devel_test — exploratory. The 5-seed expansion at fixed G5 on 2 unseen-during-selection seeds {999, 31337} (§4.11.1.2) is confirmatory; both new seeds gave K=2 > K=1 lifts (+0.0092, +0.0095) within the original 3-seed range (+0.0075 to +0.0137), bounding the selection effect. Paper should call this out explicitly to avoid the "multiple-comparison-on-devel" critique. **Caveat: devel proxy, not hidden test.** All numbers on devel_test (speaker-disjoint subsplit via stratified_grouped_split); ComParE 2017 hidden test labels are unavailable, so 0.7037 is "best achievable on devel_test under speaker-grouped CV" not a direct apples-to-apples comparison vs the 0.710 baseline (which was on the hidden test set in 2017). Paper writeup will include both numbers + the apples-to-apples caveat. **A5b K=2 FINAL LOCKED state:** cold UAR 0.7037 ± 0.0060 (5-seed, 4.30σ over K=1, every seed positive); probe (i) 0.0182; probe (ii) 0.0729; distance to 0.71 target 0.006 (within ~1σ of K=2 σ). All 2-D acceptance gates passed. **A5b K=2 = canonical late-fusion system for the paper.**
- **A5b K=2 5-seed lock (DONE; Tier-1 §4.11.1.2): K=2 lift sharpens from 1.4σ at N=3 to 4.30σ at N=5; bulletproof.** (`results/A5b_k2_5seed_lock.json`, 4.83 min total.) Headline N=5 numbers vs prior 3-seed locks: A2.5 0.6564→0.6563 ± 0.0027 (σ -29%); K=1 LOCKED (G4_gi) 0.6913→0.6934 ± 0.0064 (σ -16%); **K=2 LOCKED (G4_gi+G5_modulation) 0.7023→0.7037 ± 0.0060 (σ -21%)**; K=2−K=1 lift 0.0110±0.0032 → **0.0103±0.0024 (σ -24%; 4.30σ)**. The N=3 subset of the 5-seed run reproduces the prior 3-seed locks EXACTLY (LR probes + β-sweep are deterministic given saved A2.5 ckpts + cached pooled stats + identical seeds), so all σ-tightening comes purely from the 2 new seeds {999, 31337}. **Per-seed N=5 K=2−K=1 deltas all positive** — seed 42 +0.0117, seed 123 +0.0137, seed 7 +0.0075, seed 999 (NEW) +0.0092, seed 31337 (NEW) +0.0095. No seed produces null or reversed lift. **Mechanism stability:** β* for K=2 on the new seeds (999: β*=12, 31337: β*=8) sits in the same interior plateau as the original 3 seeds (β*=[6, 8, 8]); G4_gi+G5_modulation orthogonality holds across all 5 seeds. **τ* varies widely across seeds (-3.95 to +1.275)** — confirms that locked-β + free-τ is the right calibration discipline: τ floats with the A2.5 logit distribution per seed, β picks the same operating region across all. **Cumulative stack at N=5 (paper headline):** uniform-A2-grouped 0.6361 → A2.5 0.6563 ± 0.0027 (+0.020) → K=1 0.6934 ± 0.0064 (+0.037 over A2.5) → **K=2 0.7037 ± 0.0060 (+0.010 over K=1, +0.068 cumulative, ~17σ over leak-corrected baseline)**. **Distance to 0.71 baseline: 0.006**, within ~1σ of the K=2 standard error. **Stability finding worth a sentence in the paper writeup:** the 3-seed locks (A2.5 0.6564, K=1 0.6913, K=2 0.7023) were stable enough that the 2-seed extension didn't shift any mean by more than +0.0021. Where most paralinguistic-challenge papers report seed-fragile differences at N=3, the systematic locked-β methodology produces seed-robust headline numbers at this corpus scale. New seeds were chosen via inverse-rarity (seed=999, seed=31337 — both substantively different from the original {42, 123, 7} hash-prefix pattern) so the σ reduction isn't an artefact of a particular seed family.
- **A7c v2 (DONE; corrected re-run after reviewer methodological critique of v1) gives the canonical A7c closure verdict: `B_dann_dead_substrate_resistant`.** (`results/A7c_v2_unbottlenecked.json`, 9.44 min.) Reviewer 1 correctly identified that v1's `memorisation_dead` fail-fast verdict was over-conservative — devel 0.084 > 0.08 high threshold means the substrate HAS transferable speaker signal; the huge memo gap (+0.91) is a regularisation issue (under-regularised disc memorises easily) not a substrate-dead signal. v2 applied three corrections: (1) decision logic FIXED — devel > HIGH proceeds regardless of memo gap (memo gap = warning, not kill); (2) discriminator REGULARISED — dropout 0.3, weight_decay 1e-3, 30 epochs, per-epoch in-flight disc devel tracking; (3) λ range LOWERED — {0.0, 0.001, 0.003, 0.01, 0.03, 0.1}. Phase (a-i) re-passed with substrate_has_signal_proceed (D3 devel 0.087, memo gap warning logged). Phase (a-ii) ran the full sweep: **cls UAR, MLP probe, LR probe, AND in-flight disc devel are ALL FLAT across λ ∈ [0.0, 0.1]** — every metric within seed noise of λ=0, no detectable adversary effect on any axis. The adversary's GRL pressure is being absorbed by the layer-weights without changing the substrate's speaker-recoverability properties. **This is a STRONGER closure than v1's verdict** — DANN got a fair fight at every axis the reviewer recommended fixing AND still didn't move anything measurable. v1 verdict superseded; v1 ships in the paper as a methodology negative example (over-conservative fail-fast hides a real result), v2 ships as the canonical A7c verdict. **M13 refined**: decision logic must put `devel > HIGH` BEFORE memo-gap-as-kill in the priority order; memo gap is a regularisation warning, not a fail-fast trigger. The v1→v2 worked example is itself paper-worthy as a methodology nuance. **All within-budget A7-family options now ruled out:** v1, v2 (both) un-bottlenecked DANN, plus head-only PoC at 128-d. (b) full transformer fine-tune still out of budget. **Pivot to (c) write-up confirmed.** The methodology framework (M8/M9/M10/M11/M12/M13-refined/M14 + 3-level closure pattern + the v1→v2 disc-ceiling-decision-logic correction worked example) is the paper's load-bearing contribution.
- **A7c un-bottlenecked test (DONE; 1.70 min) closes the last A7-family option within budget — three-level closure is now categorical at EVERY substrate scope tested.** (`results/A7c_unbottlenecked.json`.) Last-shot test of whether removing the projection bottleneck creates room for adversary signal: applied the M13 pre-flight to the un-bottlenecked 4096-d fused substrate. **Result: even at 4096-d, memorisation dominates.** D2 MLP 4096→1024→210 reaches train 1.0000 / devel 0.0837 (memo gap +0.9163); D3 MLP 4096→2048→1024→210 nearly identical (devel 0.0841, gap +0.9140). Devel top-1 IS higher at 4096-d than at 128-d (0.083 vs 0.042) but memo gap is identical (+0.91 in both cases) — so ~91% of apparent speaker recovery at any substrate is chunk-by-chunk fingerprinting rather than generalising speaker structure. **D2 ≈ D3 within noise** — capacity isn't the limit; ~9% devel is the substrate's actual ceiling on transferable speaker info on URTIC at k=210. Phase (a-ii) adversarial λ-sweep skipped by the M13 fail-fast logic (memo gap > 0.5 threshold). **Final A7-family verdict: `a7c_dead_memorisation_dead`.** All within-budget A7 escapes ruled out: (a) un-bottlenecked, just tested, dead; (b) full transformer fine-tune, out of budget. Three-level closure is now categorical at every architectural and substrate scope tractable. **M14 added to §14.1**: "Speaker-probe-as-de-confounding-measurement has a fundamental noise floor on small-data corpora with coarse pseudo-speaker labels — discriminator memorisation dominates over generalising speaker structure on the substrate REGARDLESS of bottleneck." This is the deepest finding from the A7-family closure and it generalises beyond URTIC to any small-data paralinguistic corpus (the 5k-50k chunk scale that's typical for ComParE-style challenges). **Pivot to (c): write the negative-result methodology paper.** The framework is the load-bearing contribution.
- **A7 PoC + disc-ceiling diagnostic close the layer-weight-open scope: the bottleneck-confound (M10) returns at deeper scope, the adversary has no generalisable speaker signal to push against, three-level closure of the de-confounding ladder is now categorical at every frozen-backbone scope tractable in the project's compute budget.** A7 Phase 1 PoC (DANN + GRL at layer-weight-open scope, λ_max sweep {0.0, 0.01, 0.03, 0.1, 0.3, 1.0}, 3 seeds, 4.95 min, `results/A7_phase1_PoC.json`) returned `B_null` — discriminator at λ=0 trained to 0.0235 (just below the 5×-chance threshold), so the adversarial verdict was uninterpretable per the reflection's diagnostic. Disc-ceiling diagnostic (DONE; `results/A7_disc_ceiling.json`, 1.30 min): strongest discriminator (MLP 128 → 1024 → 512 → 210, 200 epochs) reaches **best devel top1 = 0.0422 ± 0.0007 with best train top1 = 0.9521 — memorisation gap +0.910 (the M12 case)**. Even D2 (MLP 512/512) shows the same pattern (gap +0.860), and linear LR matches the deepest MLP within noise (devel 0.0434 vs 0.0422) — so there's no nonlinear speaker structure for the deeper discriminators to find. **Decision: `memorisation_a7_dead`.** A7 at layer-weight-open scope is closed. Two findings stack independently to support this:

- **Finding 1 (instrumentation):** the in-loop disc at the A7 PoC's lr 1e-4 / 20-epoch / moving-target dynamics reached only ~5× chance train (0.0235), confirming the B_null verdict-classifier's call. The PoC λ-sweep numbers are uninterpretable.
- **Finding 2 (substrate, the load-bearing one):** decoupling disc training from in-loop dynamics — the disc-ceiling diagnostic with frozen z + properly-strong disc + 200 epochs at lr 1e-3 — STILL gives best devel 0.042 with memo gap +0.91. The substrate has no transferable speaker info for any adversary to push against.

The two findings stack: even if Finding 1 were fixed (a longer/stronger in-loop disc), Finding 2 says it wouldn't help — the bottleneck has already done the work. This is M13 (disc-ceiling diagnostic must run before any λ-sweep verdict can be interpreted) in action. **Three-level de-confounding ladder, final state:** data (A5.5 = M9 narrow window empty, modest UAR / null on probe), representation head-only (A6 = M10 bottleneck explains apparent drop + M11 subtractive contrastive across 3 recipes), representation layer-weight-open with adversary (A7 = M10 + M12 memorisation-dead). All closed at the architectural levels and compute scopes tractable in the project's budget. Remaining options: (a) un-bottlenecked adversary at 4096-d substrate (~30-60 min, optional follow-up — tests whether removing the projection bottleneck creates room for the adversary); (b) full transformer fine-tune (multi-hour-to-day, out of budget); (c) accept the closure and write the negative-result methodology paper anchored on M8/M9/M10/M11/M12 + the systematic three-level closure pattern. (c) is publishable as-is; (a) is a cheap last-shot to add an extra ablation row before pivoting to (c).
- **A6b combined-loss λ-sweep (DONE) closes the A6 head-only scope across all tested recipes; pure cold-CE Pareto-dominates monotonically. The "λ=0 is monotonically best on every metric" finding upgrades the original PoC failure into a publishable mechanism story.** (`results/A6b_phase1_combined_lambda_sweep.json`, 5 λ × 3 seeds, 2.66 min.) After the §4.9.1.1 controls disproved the strong A6 claim, the surviving hypothesis was "contrastive as a regulariser on top of cold-CE-anchored bottleneck might still help." Tested by jointly training projection + cold linear head with `L = L_cold_CE + λ · L_supcon_speaker_masked`, sweeping λ ∈ {0.0, 0.05, 0.1, 0.25, 0.5}. **Result: λ=0 (pure cold-CE) is monotonically the best on every metric.** MLP probe worsens monotonically with λ (0.0408 → 0.0428 → 0.0435 → 0.0451 → 0.0462); LR probe same pattern (0.0371 → 0.0419, with a tiny noise dip at λ=0.1); cold UAR drops at any λ > 0 and stays flat-low (0.5994–0.6028 vs λ=0's 0.6128); class margin shrinks monotonically (+0.350 → +0.107). **Contrastive class-pressure is purely subtractive when added to a CE-anchored bottleneck at head-only scope** — when CE has already maximised class margin (+0.35), contrastive *competes* with rather than complements that geometry. **4-step causal chain (the publishable mechanism):** (1) CE on the 128-d projection bottleneck produces sharply class-separated geometry where speaker info is mostly compressed away by the bottleneck itself (M10); (2) adding SupCon class-pressure flattens within-class diversity — CE already produced separation, contrastive adds no new structure; (3) the flattened diversity includes cold-relevant variation that helped CE generalise — squeezing it out drops cold UAR; (4) SupCon treats "different cold class" as separation pressure regardless of speaker, so it pulls same-speaker different-cold pairs apart, *re-introducing speaker-correlated variance* and raising the MLP probe. Falsifiable, recipe-fundamental, and transferable to the foundation-model + small-head paralinguistic literature (M11). **Architectural argument against scaling up:** the failure isn't scope-limited — it's recipe-fundamental at any scope where CE anchors the optimization. (A-i) layer-weight-open gives the contrastive loss more parameters but doesn't change the underlying CE+bottleneck-already-compressed interaction. Don't run A-i; pivot directly to A7. **Surprising-takeaway framing for the paper:** *"Pure cross-entropy on the 128-d projection bottleneck is the Pareto-best head-only recipe — better than any contrastive variant we tested."* Decision: `contrastive_dead_pivot_to_a7`. **Sanity verified**: A6b λ=0.0 reproduces the cached cold-CE control within seed noise — code path validated. **Across all 3 tested A6 recipes** (pure speaker-masked SupCon, vanilla SupCon, combined CE+SupCon λ-sweep) **at head-only scope, none beats pure cold-CE on de-confounding while preserving cold UAR.** This is a clean publishable negative result — the methodology yielded a categorical closure of the head-only scope, not just a single-recipe failure. (A-i) layer-weight-open scope still untested but low expected payoff given the systematic failure pattern. Pivot to A7 (gradient-level adversary, plan §4.10 to be scoped) is the right move.
- **A6 head-only PoC mechanism LOCKED as illusory — bottleneck explains the apparent LR-probe drop. (M10 worth flagging in the paper.)** Phase 1 PoC (`results/A6_phase1_PoC.json`, 3 seeds, 0.59 min): fresh projection MLP (4096 → 512 → 128, L2-normalised) on top of A2.5's frozen scaler + layer-weights, supervised contrastive (Khosla 2020) with positives = same Cold + different pseudo-speaker. Result: MLP probe 0.0477 ± 0.0008 (vs A2.5 ref 0.0501 — barely moved); LR probe 0.0410 ± 0.0024 (vs A2.5 ref 0.0725 — appeared dramatic, ~13σ on LR seed-σ); cold-LR UAR on z 0.5988 ± 0.0085; class margin +0.0594 ± 0.0065. Strict gate fired branch (D) for 2/3 seeds; reflection raised the bottleneck-confound concern (A2.5 LR baseline is on 4096-d fused; A6 LR is on 128-d L2-normalised z — 32× dim reduction + metric change can drop LR-recoverable speaker info independent of training). **Controls** (`results/A6_phase1_PoC_controls.json`, 3 controls × 3 seeds, 1.62 min): C1=random untrained projection on the same 128-d substrate gave LR 0.0427 (within 0.0017 of A6's 0.0410 — bottleneck does ~all of the apparent LR de-confounding). C2=cold-CE only (no contrastive, no speaker-masking) gave LR 0.0373, MLP 0.0413, cold UAR 0.6124, margin +0.350 — Pareto-dominates A6 on every metric, with supervised cold pressure subsuming whatever the speaker-aware objective was supposed to provide. C3=vanilla SupCon (mask_speakers=False) gave LR 0.0395 — slightly *better* than A6's 0.0410, meaning speaker-masking is mildly *counterproductive* (reduces positive count without buying de-confounding back). **Decision: `bottleneck_explains_lr_drop`. A6 head-only mechanism is illusory.** The 4096 → 128 + L2-norm bottleneck does essentially all of the apparent LR-substrate de-confounding; the contrastive objective adds nothing on top; speaker-masking actively hurts. **Phase 2 implications:** (A-i) layer-weight-open variant is available IF the user wants 2-4 hr GPU spend, but MUST include the same control discipline (random-projection-at-layer-weight-open + cold-CE-at-layer-weight-open) before claiming any verdict. (A-ii) full transformer fine-tune is doubly disqualified for now: head-only mechanism is illusory, and A-i needs its own controls before justifying multi-hour-to-day spend. **Recommendation: pivot to A7** (gradient-level adversary) unless user wants to explore the layer-weight A-i variant with full controls. **Methodology lesson (M10 — see §14):** probe-substrate dimensionality is itself a confound for de-confounding measurements; comparing a probe trained on a high-dim baseline against the same probe on a low-dim post-projection conflates information loss with de-confounding. De-confounding rungs that introduce a dimensionality bottleneck MUST include a random-projection control at the same target dim. Generalises beyond URTIC — anyone doing representation-level de-confounding on FM features with a projection MLP should run this control before claiming mechanism activation. Without it, the user nearly escalated to Phase 2 (~2-4 hr) on a false-positive belief; the controls (~1.6 min) caught it.
- **(DONE) A5.5 LOCKED at conservative-α embedding mixup; α-axis closed.** Audio-splicing v1 killed by Phase 3.5 (M8 self-splice diagnostic). Pivoted to embedding mixup; Plan B Phase 2 + audit BOTH PASS (detector UAR 0.503; mix-bit-as-cold 0.500 by class-balanced construction). **Conservative α ∈ [0.70, 0.85] (LOCKED as canonical):** UAR 0.6624 ± 0.0031 (Δ +0.006, ~1.6σ), recall pattern more cold-balanced (recC +0.04, recNC -0.02), MLP probe ~unchanged (Δ +0.0005), LR probe slightly UP (Δ +0.003), cos(init, final) ≈ 0.9999 — PARTIAL PASS (UAR floor PASS, probe drop FAIL). **Aggressive α ∈ [0.50, 0.70] (ABLATION, ran):** UAR 0.6397 ± 0.0186 (Δ -0.017, σ 6×), MLP probe 0.0485 (Δ -0.0017 within noise), LR probe 0.0735 (Δ +0.0010 within noise), cos(init, final) = 0.99998 (EVEN MORE locked at A2.5 init), best_epoch = {1, 1, 1} — branch (d, unexpected): UAR drops AND probe doesn't drop. **Verdict: A5.5 = conservative α canonical; aggressive α ablation closes the α-axis (M9 narrow-window-empty).** De-confounding load moves to A6 (representation-level) and A7 (gradient-level). See (M9) for the narrow-window-empty mechanism and (A4) for the locked rung writeup with the variant comparison table.
- **(DONE-as-disqualified) A6 head-only Phase 1 PoC + controls + combined-loss λ-sweep = LOCKED illusory across all tested recipes** (`results/A6_phase1_PoC.json` + `results/A6_phase1_PoC_controls.json` + `results/A6b_phase1_combined_lambda_sweep.json`, see M10). Three independent recipes tested at head-only scope: (1) speaker-masked SupCon alone (PoC), (2) vanilla SupCon alone (control C3), (3) combined cold-CE + speaker-masked SupCon at λ ∈ {0.0, 0.05, 0.1, 0.25, 0.5} (A6b). All fail; pure cold-CE Pareto-dominates all of them. The combined-loss sweep showed that λ=0 is monotonically the best on every metric — contrastive class-pressure is purely subtractive when added to a CE-anchored bottleneck. **A6 head-only is fully closed.** (A-i) layer-weight-open variant available IF user wants 2-4 hr GPU spend with full control discipline; (A-ii) full transformer fine-tune doubly disqualified for now. **Recommendation: pivot to A7** (gradient-level adversary) unless user wants to explore A-i with controls.
- **(DONE-as-disqualified) A7 DANN adversary at layer-weight-open scope = LOCKED dead** (`results/A7_phase1_PoC.json` + `results/A7_disc_ceiling.json`, see M12). PoC `B_null` (disc at λ=0 = 0.0235, just below 5× chance) → disc-ceiling diagnostic (D3 1024→512→210, 200 epochs): best devel 0.0422, best train 0.9521, memorisation gap +0.910. The substrate has no transferable speaker info for any adversary to push against — bottleneck-confound (M10) returns at layer-weight-open scope. Three-level closure of the de-confounding ladder is now categorical at every tractable frozen-backbone scope.
- **(DONE-as-disqualified) A7c un-bottlenecked variant on 4096-d substrate = LOCKED dead** (`results/A7c_unbottlenecked.json`, see M14). M13 pre-flight on 4096-d substrate: D3 best devel 0.0832 (numerically clears 0.08 threshold) BUT memo gap +0.91 (M14 substrate-memorisation-dominated case) → fail-fast skip Phase (a-ii). Devel top-1 IS higher at 4096-d than at 128-d (0.083 vs 0.042) but memorisation pattern identical, so the apparent improvement is chunk fingerprinting not generalising signal. D2 ≈ D3 — capacity isn't the limit; ~9% devel is URTIC's substrate ceiling on transferable speaker info at k=210.
- **All within-budget A7-family options now ruled out.** (a) un-bottlenecked just tested, dead. (b) full transformer fine-tune out of project budget. **Pivot to (c): write the negative-result methodology paper** anchored on M8/M9/M10/M11/M12/M13/M14 + the systematic three-level closure pattern. The methodology framework + 7 paper-ready negative-control disciplines is the load-bearing contribution. The de-confounding ladder is **categorically closed at every architectural level and substrate scope tractable in budget**, with the deepest finding being M14: speaker-probe top-1 has a memorisation noise floor on small-data paralinguistic corpora that caps interpretable de-confounding measurement regardless of bottleneck.

**Optional follow-ups (none load-bearing for the paper):**

- **Late-layers-only A2 baseline** (restrict to L18-24) — less urgent now that the lr × init grid showed the optimizer prefers early layers (L0/L4/L5) at higher lr. A late-layers-only A2 would test "does forcing late layers improve speaker honesty without sacrificing cold UAR much?" — diagnostic value, not a new rung.
- **β-sweep on uniform-A2 anchor as a control** — does the original A5b (A2-a/A2-b) PASS hold under fine β sweep on uniform anchor? If β=1.00 was the right hyperparameter for that anchor (likely, given the PASS), no new audit needed. Diagnostic only.
- **5-seed extended β-sweep** — current extended sweep is 3 seeds; extending to 5 would tighten σ on the locked K=1 fused number for paper-prep stage.
